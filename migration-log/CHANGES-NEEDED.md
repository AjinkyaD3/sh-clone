# Changes Needed — `/new/*` pages vs live WordPress site

## RowN.tsx renamed to meaningful names sitewide — 2026-09-13 — DONE

Standing TODO from the sitewide section split (2026-09-12) finally done properly, not rushed - the user asked for the full sweep rather than the incremental "rename it when you touch it anyway" plan.

**Method**: `scripts/rename_rows.js` infers a name from each component's own first heading (`<h1>`-`<h6>`), concatenating all its text chunks (a heading split across `<br />` into two template-literal chunks was truncating badly before this fix - e.g. "What are the key" / "aspects of safe rooms?" was collapsing to just "WhatKey"). Falls back to a small set of structural signals (testimonials, forms, galleries, video embeds, spec tables, FAQ accordions, the new `SystemOverviewModal`/`GetAQuote`/`TrustLogos`) when there's no heading. **Deliberately conservative**: 89 files had no reliable signal at all and were left as `RowN` rather than guessing - matching the standing principle that a wrong name is worse than an honest generic one.

**Piloted before the full run**, per this project's own established practice: dry-run on `/trade`, `/doors/profile-doors/fuego-fire`, `/doors/panic-room-doors`, `/garage-doors/sectional-garage-doors`, `/security-levels` first, and caught two real bugs from that review before running site-wide:
1. The `<br />`-split heading truncation above.
2. Repeated-prefix headings (e.g. "Security sectional garage doors London: R40 Sectional" vs "...: TL Sectional" vs "...: SHD Side Hinged Doors") were all truncating to the identical shared prefix before reaching the actually-distinguishing part, since it's within a 5-word cap. Fixed by preferring whatever follows a colon, when there is one.

**Full run**: 275 files renamed, 89 kept as `RowN` (honest skip), across all 34 split pages. Reviewed the complete output line by line (not just spot-checked) before committing, and caught two more real issues that direct read-through - not the pilot - surfaced:
- `door-styles/georgian-doors`' own gallery section got named `EdwardianDoorGallery.tsx` - not a script bug, it's faithfully reflecting a real, already-documented pre-existing typo on the live site itself (the Georgian gallery heading literally says "Edwardian door gallery" - see the "Pre-existing content bugs" section of this file). The visible content typo is correctly left untouched per this project's standing precedent, but the *file name* doesn't need to inherit that confusion for a future developer - manually renamed to `GeorgianDoorGallery.tsx`.
- One file in `components/windows/` ended up `LetSSecurePropertyTogether.tsx` (capital S) instead of matching the ~15 other instances of `LetsSecurePropertyTogether.tsx` elsewhere - a stray non-ASCII apostrophe character in that one heading wasn't in the character-strip set, splitting "Let's" into two words. Manually corrected for consistency.
- **Real crash caught by `tsc`, not assumed away**: 4 generated names started with a digit (`4ReasonsWhyShouldYouConsider`, `200Designs`, `1ManualControlSecurityShutters`, `2UsingElectricDrivesSecurityShutters`) - invalid JS/component identifiers, which `tsc --noEmit` correctly refused to compile. Fixed all 4 by hand (e.g. `FourReasonsToConsider`, `TwoHundredPlusDesigns`) and added a guard to the script itself (strip a purely-numeric leading word) so a future incremental rename doesn't reintroduce this.

**Verified for real, not just "tsc is clean"**: after a full `.next` cache clear and dev server restart (a mass rename this size risked stale file-watcher state), curl-checked all 37 real routes for `200` - all passed - then loaded `/door-styles/georgian-doors` in an actual browser and confirmed both that its content is byte-for-byte unchanged (still shows the "Edwardian door gallery" typo, proving only the file name moved) and zero console errors.

`tsc --noEmit` clean. Not committed yet.

## Open Graph / Twitter Card metadata sitewide — 2026-09-13 — DONE

Every page now has real `openGraph`/`twitter` metadata instead of a bare link on share. Default set in `app/layout.tsx` (site name, `en_GB`, a real photo - a high-res front-door hallway shot already in `public/legacy-assets`, used as the fallback social image), then every page's own `title`/`description` reused for its own `openGraph`/`twitter` block via a codemod (`scripts/add_open_graph.js`) rather than hand-writing 37 near-identical blocks.

**Real bug caught by actually checking the rendered `<meta>` tags, not just trusting the code**: the first pass left every page's `og:image` missing. Next.js does not deep-merge a child page's `openGraph` object into the layout's default - if a page declares its own `openGraph` at all (even just for a page-specific title), the whole object replaces the layout's, silently dropping `images` since the page's block didn't include it. Fixed by creating `lib/seo.ts` (one shared `DEFAULT_OG_IMAGE` constant) and a follow-up codemod (`scripts/add_og_image.js`) that adds it to every page's own block too, rather than relying on inheritance that doesn't actually happen.

**Second real bug, same root cause (checking output, not just re-running the script and assuming success)**: `app/page.tsx` (the homepage) was silently skipped by both codemods - `git ls-files "app/**/page.tsx"` does not match `app/page.tsx` itself (the pattern requires at least one subdirectory), so the homepage kept the generic sitewide default (`og:title: "Secure House"`) instead of its own real title, and nobody would have noticed without checking the actual rendered meta tags per page rather than trusting the script's own "Updated: 36" count. Fixed by hand for this one file.

Verified: checked rendered `<meta property="og:...">` tags directly in the browser on the homepage and `/contact-us` - correct page-specific title/description, and `og:image` resolving (via `metadataBase`) to a real, servable image on all pages. `tsc --noEmit` clean. Not committed yet.

## Alignment gap re-checked (resolved), EDD CSS removed — 2026-09-13

**~11px category-page alignment gap - re-checked, no longer present.** User asked to see this side-by-side before deciding whether to fix it. Measured `getBoundingClientRect().left` of the `/garage-doors` heading against the live site at an identical, real 2560px viewport width (not the original 1425px comparison) - both now report the exact same value, `99.109375`. Whatever caused the original difference has already resolved itself as a side effect of other layout work done earlier this session (probably one of the many padding/spacing changes) - nothing left to fix here.

**Hero entrance fade-in animation - explicitly deprioritized by user ("don't worry about the fade in thing")** after seeing the side-by-side comparison (live site mid-fade vs ours snapping instantly). Not fixed, not going to be - closing this one out per instruction rather than leaving it ambiguously open.

**Dead Easy-Digital-Downloads CSS `<link>` tags removed from all 34 real pages.** Every page loaded two stylesheets (`edd-blocks.css`, `edd.min.css`) for a WordPress e-commerce plugin with zero relevance to a door manufacturer - pure leftover from the original theme setup, blocking a small amount of render for no benefit. Confirmed the lines were consistent (only a `?ver=3.6.9` query-string variant) before a blind removal, then stripped any line containing `easy-digital-downloads` from every non-legacy `app/**/page.tsx`. Purely mechanical - took about 2 minutes including verification. Verified: zero `easy-digital-downloads` references left anywhere outside `app/legacy`, `tsc --noEmit` clean, spot-checked `/about-us` loads with zero console errors and zero EDD `<link>` elements in the DOM.

Not committed yet.

## Security headers, Door Styles links, .heic photos, profile-door PDFs — 2026-09-13 — DONE

Four items from the standing punch list, all confirmed done with real verification (not assumed).

**1. Security response headers.** Added `headers()` to `next.config.ts`: `X-Frame-Options`, `X-Content-Type-Options: nosniff`, `Referrer-Policy`, `Permissions-Policy` on every route, plus a real `Content-Security-Policy` - but CSP only in production (`NODE_ENV === "production"`), since Next dev mode's Fast Refresh needs `eval` and a dev-server websocket a strict CSP would block for no real benefit (dev never faces real visitors). The CSP's allowed domains were built by grepping the actual codebase for every script/style/font/image/iframe source in use, not guessed - confirmed via that grep that every `<iframe>` on the whole site is either a YouTube embed or Tawk.to's own widget. `'unsafe-inline'` is required (not a shortcut) for both `script-src` and `style-src`: this migration ports Avada's markup verbatim, which means thousands of inline `style={{}}` objects (CSP's `style-src` governs the `style=""` attribute too) plus legitimate inline bootstrap scripts (GTM, Clarity, Tawk.to). **Verified for real**: ran an actual production build (`npm run build && npm run start` - headers don't apply in `next dev`), confirmed via `curl -I` that all headers including the full CSP are actually sent, then browsed the site with the real CSP active and confirmed zero `Content-Security-Policy` console violations - Tawk.to chat loaded and opened correctly, a YouTube product-video embed rendered, and the Get-a-Quote panel worked end-to-end.

**2. Door Styles hub links.** `components/door-styles/Row4.tsx`: wrapped each of the 4 cards' `fusion-column-inner-bg-image` span in a `<Link>` (same `fusion-column-anchor` pattern already used and verified for the `/grilles-shutters`, `/garage-doors`, and `/windows` card fixes earlier this session) pointing at `/door-styles/french-doors/`, `/victorian-doors/`, `/edwardian-doors/`, `/georgian-doors/`. Verified by reading the actual rendered `href`s back from the DOM and clicking through to `/door-styles/french-doors`.

**3. The 3 `.heic` images on `/garage-doors/side-hinged-garage-doors`.** Confirmed first, not assumed: this exact bug exists on the live WordPress site too (`naturalWidth`/`naturalHeight` both `1`, i.e. broken there as well) - not a migration regression, a real client content bug (raw iPhone photos uploaded in a format no browser can display). Since the original `.heic` files were already present locally, converted them for real using their actual photo data via `heic-convert` (added as a dev dependency, `scripts/convert_heic_images.js`) rather than substituting placeholder or stock images - visually confirmed all 3 converted `.jpg`s show the correct, real content (two garage-door photos, one mews-house front). Found and fixed a **second** bug underneath the first while wiring up the `.jpg` references: all 3 `<img>` tags were still stuck on Avada's dead lazy-load placeholder (`src="data:image/gif;base64,..."`, real path only in `data-orig-src`, same "lazyload never runs here" pattern documented elsewhere in this project) - fixed by setting `src` directly, matching how every other (working) image on that same page is already structured. Verified via `naturalWidth`/`naturalHeight` in the live DOM (all 3 now report their true full resolution, e.g. 4284×5712) and a full-gallery screenshot. Removed the now-unused `sharp` dependency afterward - it got installed alongside `heic-convert` on the assumption it'd be needed for resizing, but the conversion never used it.

**4. Profile-door PDF spec sheets - 13 files across 3 pages**, downloaded from the live site with explicit user go-ahead first. Investigated before downloading anything: all 3 pages (Fuego Fire, Presto Bullet Proof, Unico Slim Line) already have a "System overview" button in the ported JSX (`components/doors/profile-doors/*/Row5.tsx`), styled correctly - the scrape captured the button but not the off-canvas panel it was supposed to open (`href="#awb-oc__5440"` etc., an ID Avada's own JS builds at runtime, which never runs here), so it was a dead link. Confirmed `Stainless Steel` has no product-specific PDFs on the live site either - nothing to add there. Built one reusable component, `components/SystemOverviewModal.tsx` (matches the live site's actual modal design: centered panel, heading, a list of real labeled PDF links), parameterized by `heading` + `items`, and used it to replace each dead anchor in the 3 pages' `Row5.tsx`. Downloaded all 13 real PDFs (fire-rating system overviews, glazing/hardware spec posters, 44KB–1.25MB each) to `public/downloads/profile-doors/<page>/`, verified every file is a genuine PDF (not an error page) via `file`, and sourced each button's exact label text directly from the live site's own modal rather than inventing generic ones. Also confirmed the "quote form" half of this task item is already covered - the live site's per-page quote form has the identical fields to the sitewide Get-a-Quote panel already built. **Verified for real**: opened each of the 3 modals in the browser and fetched every link with a HEAD request, confirming all 13 return `200` with `content-type: application/pdf`; confirmed Escape closes the modal and restores body scroll.

`tsc --noEmit` clean throughout. Not committed yet.

## Custom 404/500 pages + redirect decision — 2026-09-13 — DONE

User decided: no redirects for the ~150 archived URLs (those pages are empty, and the archive already lives outside the project as a reference) - closing that item as a deliberate decision, not a gap. Custom error pages: user asked to build them and explain what they do.

**Built three files**, each a distinct Next.js App Router convention:
- **`app/not-found.tsx`** - renders whenever a route doesn't match anything (a mistyped URL, an old bookmark, one of the ~150 archived URLs). This is the one that matters most day-to-day.
- **`app/error.tsx`** - a client component (required by Next.js, since it needs interactivity) that catches an unexpected runtime error anywhere under the root layout - e.g. a page component throwing during render. Gets a `reset()` function from Next.js that re-renders just that segment without a full page reload, wired to a real "Try again" button.
- **`app/global-error.tsx`** - the one edge case the other two can't cover: an error thrown by the root layout itself (`Header`, `Footer`, the big inline `<style>` block, the Tawk.to script, etc.). Next.js requires this file to render its own complete `<html>/<body>`, since the layout that would normally provide those is exactly what's assumed broken - kept deliberately minimal and import-free (no `Header`/`Footer`/`Link`) for the same reason.

All three styled on-brand (cream `#f5efe9` background, navy `#1c1e36` text, Playfair Display heading, matching the Get-a-Quote panel's palette) rather than left as generic Next.js defaults - the 404 page links to every primary category plus the phone number, so a lost visitor has somewhere real to go instead of a dead end.

**Real bug caught during verification, not assumed away**: `not-found.tsx` and `error.tsx` both render inside the root layout, so `Header`/`Footer`/the chat widget/the Get-a-Quote tab all appear automatically - but the first version rendered with the header nav and footer completely unstyled (raw bullet-list menu, overlapping footer text). This is the *exact same root cause* as the earlier `/blog` bug already documented in this file: every Avada-scraped page loads its own compiled `fusion-styles/[hash].min.css` bundle, which carries a large shared base-theme payload that `Header`/`Footer` depend on - and these two new files aren't scraped pages, so they never had it. Fixed the same way `/blog` was fixed: added the homepage's stylesheet link to both files. A second, smaller bug surfaced once that link was in place: the loaded bundle's own global `h1` rule (likely meant for hero headings) was setting the heading to white, rendering it as an invisible ghost against the cream background - fixed with a scoped `!important` override, the same technique already used elsewhere in this project for identical cascade conflicts.

Verified for real: screenshotted both pages with the full site chrome correctly styled: took a direct 404 hit on a nonexistent URL and confirmed the page renders with working links (clicked "Doors," landed on `/doors`); temporarily added a route that throws inside its page component, confirmed `error.tsx` renders (screenshotted) with a working "Try again" button, then deleted the test route. `tsc --noEmit` clean throughout.

## Repo cleanup: legacy/scratch/unused uploads moved out, cheerio relocated — 2026-09-13 — DONE

User asked to move (not delete) the dead weight identified in the earlier Antigravity audit out of the project entirely, plus fix the misplaced `cheerio` dependency. Explicitly asked to leave `chat.txt` and `all url.txt` alone.

- **`app/legacy/` (71 files, 6.7MB) moved to `../archive/app-legacy`** (i.e. out of `secure-house-nextjs/` entirely, into the parent `SH NEXT JS` folder). These were the old pre-JSX-conversion duplicate routes, already disallowed in `robots.ts` and excluded from `sitemap.ts` - kept as an offline reference rather than deleted, since nothing about their content was ever verified as safe to lose. Removed the now-pointless `/legacy` disallow rule from `app/robots.ts` and the `entry.name === "legacy"` skip branch from `app/sitemap.ts`'s directory walk, since the folder no longer exists for either to ever encounter.
- **`scratch/` (364MB) moved to `../archive/scratch`.** This was mid-migration backup/scrap work (raw video originals, old HTML backups, one-off scripts) that was never in `.gitignore` despite being untracked-in-spirit - added `/scratch/` to `.gitignore` so it can't silently end up tracked again if it's ever recreated locally.
- **166 unreferenced upload files (62MB) moved to `../archive/unreferenced-uploads/`, preserving their original folder structure.** Wrote `scripts/find_unreferenced_uploads.js` to find these properly rather than trusting the earlier audit's "221" figure blindly: it builds a haystack of every `.tsx`/`.ts`/`.css`/`.html` file in `app/`, `components/`, and every compiled CSS bundle under `public/` (images are referenced from inside those compiled stylesheets too, not just JSX), excluding `app/legacy` since it was leaving in the same pass, then flags any `public/legacy-assets/uploads/*` file whose basename never appears anywhere in that haystack. Got 166, not 221 - a different (and, since it's ours, independently verified) method, not a discrepancy worth chasing. The full list is preserved at `../archive/unreferenced-uploads-list.txt`. Spot-checked the list before moving anything (blog-post images for since-archived articles - `fire_doors_...`, `Default_...` stock-photo-style names - consistent with "leftovers from the 150 archived pages," not real product photos) and confirmed zero dynamic/template-literal-constructed image paths exist anywhere in the codebase that a static text scan could miss.
- **`cheerio` moved from `dependencies` to `devDependencies`** in `package.json` - confirmed via `grep` it's only ever imported by offline scripts (`scripts/html_to_jsx.js` and 6 others), never by anything under `app/` or `components/`, so it has no business shipping in the production bundle. `npm install` re-run to update `package-lock.json` to match.

**Verified nothing broke, not just "should be fine":** stopped the dev server first (it was holding file-watcher locks on `app/`, which blocked the initial `mv`), did all the moves, cleared the stale `.next/` build cache (its auto-generated route-type file still referenced the old `/legacy` routes, which briefly looked like real `tsc` errors), restarted the dev server, and confirmed: `tsc --noEmit` clean; `/legacy/doors/bullet-proof-doors` now correctly 404s; `/`, `/garage-doors`, and `/contact-us` all still 200; `robots.txt` and `sitemap.xml` no longer mention `/legacy` anywhere; zero broken `<img>` elements on the homepage (`naturalWidth === 0` check); confirmed no dynamic upload-path construction exists that the static reference-scan could have missed.

`chat.txt` and `all url.txt` deliberately left untouched at the repo root, per explicit instruction.

## SEO: canonical domain fixed + real audit — 2026-09-13 — DOMAIN DONE, findings below need decisions

**Domain fix.** Every `alternates.canonical` (69 files, including `app/legacy/*`), `app/sitemap.ts`'s `BASE_URL`, and `app/robots.ts`'s `sitemap:` field pointed at `https://secure-house-next-js.vercel.app` - the Vercel preview domain, not the real one. Replaced all of it with `https://secure-house.co.uk` (plain string replacement in metadata only - no `<Link>` component was touched; internal navigation still uses relative paths via `next/link`, as it should, so client-side routing keeps working regardless of which domain the site is eventually served from). Also added `metadataBase: new URL("https://secure-house.co.uk")` to the root layout's metadata, which was missing entirely - without it, any future relative Open Graph/Twitter image path wouldn't resolve to a real URL.

**Real audit - what's actually good vs what's missing:**

Already solid, confirmed by checking rather than assuming:
- Per-page `title` + `description` + canonical on effectively every real route (found and fixed the only 2 gaps: `app/door-styles/edwardian-doors` and `app/door-styles/french-doors` had no `metadata` export at all - fixed, matching the pattern every sibling door-style page already uses).
- `robots.ts` already correctly disallows `/legacy` and points to a real sitemap; `sitemap.ts` already correctly walks `app/` and excludes `/legacy` and dynamic route segments.
- Some pages carry real `FAQPage` JSON-LD structured data (schema.org), ported from the original site - e.g. `components/garage-doors/Row11.tsx`, `app/door-styles/georgian-doors/page.tsx`.
- No indexation risk from the `/legacy/*` duplicate-content tree - it's both disallowed in robots.txt and excluded from the sitemap.

**Gaps found, not yet fixed - need a decision, not just code:**
1. **No Open Graph or Twitter Card metadata anywhere** - zero `openGraph`/`twitter` fields in any page's metadata, and none at the root level either. Concretely: sharing any page link on WhatsApp, Facebook, LinkedIn, or iMessage today shows no image, no title, no description - just a bare link. This is probably the single highest-impact gap: it doesn't move Google ranking, but it visibly hurts every social share and every internal team member pasting a link in Slack/WhatsApp to a client.
2. **Most pages have no real `<h1>`.** Checked every page's actual rendered heading levels (not just the page.tsx wrapper - the real content lives in the split `components/<page>/RowN.tsx` files): `about-us`, `projects`, `security-levels`, and `trade` have zero `<h1>` anywhere on the page - their main heading renders as `<h2>`/`<h3>` instead (an Avada styling artifact, not a deliberate SEO choice). Google still ranks pages without an `<h1>`, but a correct single `<h1>` matching the page's primary keyword is one of the cheapest, most reliable on-page SEO signals there is - this is a real, fixable gap, not a nitpick.
3. **No sitewide Organization/LocalBusiness structured data.** The FAQ schema that exists is good, but there's nothing telling Google this is a real UK business - no NAP (name/address/phone), no `LocalBusiness` or `Organization` JSON-LD with logo, sameAs (social links), opening hours, etc. This is what can earn a Knowledge Panel / rich business result for "Secure House" searches.
4. **Lighthouse/Core Web Vitals never measured** (standing item, not new) - page speed is a confirmed Google ranking factor and nobody has checked where this site actually stands.
5. **GTM and Microsoft Clarity are still placeholder IDs** (`GTM-XXXXXXX`, `CLARITY-XXXXXXX`, standing item) - not an SEO factor directly, but means there is currently no real analytics or search-query data being collected at all.

Not committed yet.

## Enquiry backend (Resend) + homepage spacing tweaks — 2026-09-13 — DONE (backend needs a real API key)

**Enquiry backend.** Both `/contact-us` and the new Get-a-Quote panel had no working submission path (the contact form posted `action="/contact-us" method="post"` to itself with no handler; Get a Quote just set local state). Built one shared route, `app/api/enquiry/route.ts`, using the `resend` package (added to `package.json`), differentiated by a `formType: "contact" | "quote"` field so one endpoint serves both forms instead of two near-identical ones. Validates required fields server-side before touching Resend at all.

User doesn't have a Resend API key yet - rather than block on that, the route reads `RESEND_API_KEY` from env and, when it's unset, returns a clean 503 with "Email sending is not configured yet. Please call us instead." instead of crashing or silently no-opping. Documented in a new `.env.example` (added `!.env.example` to `.gitignore` since `.env*` is otherwise fully ignored) along with `CONTACT_TO_EMAIL` and `CONTACT_FROM_EMAIL`. Once a real key is dropped into `.env.local`, both forms work with no code changes.

Wired up both forms to actually call it:
- `components/GetAQuote.tsx` - form now POSTs to `/api/enquiry`, shows a real loading state ("Sending…", disabled button) and its own inline error message on failure, and the existing "Thank you" state only shows on a genuine 200.
- `components/contact-us/Row4.tsx` - converted to a client component with the same fetch-on-submit pattern. Found and fixed a real bug while wiring this up: the two response alert `<div>`s in the ported markup (`fusion-form-response-success` / `-error`) are hidden by a global CSS rule (`display: none`) that Avada's own JS would normally clear - since that JS never runs here, even conditionally rendering the right one in React left it invisible (`display:none` from the stylesheet, confirmed via `getComputedStyle`). Fixed by adding an explicit inline `display: block` on both, since React's conditional rendering is now the only thing controlling visibility. Verified end-to-end: submitting with no API key shows the real (if plainly-styled - the `.fusion-alert` color classes aren't loaded in this migration, cosmetic only) error banner; the Get-a-Quote panel's own styled error shows correctly.

**Homepage spacing, on request** - both requested as "reduce, don't remove":
- `components/home/OurProjects.tsx`: `--awb-padding-top` 160px → 130px. (This matched the live site's own value exactly before the change - not a regression, just judged too much air and trimmed on request.)
- `components/home/TrustLogos.tsx`: `--awb-padding-bottom` 67px → 35px, tightening the gap between the trust-badge row and the footer.

**Footer alignment - checked, no bug found.** Verified via `getBoundingClientRect` that the three footer link-column headings ("Quick Links", "Our Services", "About Us") sit on the identical top offset and are evenly spaced (~487px apart), with matching computed font-weight/size/color despite "Our Services" being marked up as a bare `<li>` where the other two use a `<span class="menu-text">` wrapper (a markup inconsistency worth normalizing someday, but it renders identically today - not a visible bug). Copyright bar and social icons row read correctly centered in the screenshot. Nothing to fix here right now.

`tsc --noEmit` clean. Not committed yet.

## Fit-and-finish / UX audit vs live site — 2026-09-13 — Tawk.to + Get a Quote DONE, rest is a punch list

User asked for a broad pass on "fit and finish, alignment, UX and user journey" across the whole site, specifically calling out the live site's Tawk.to chat widget and asking what else the original site does that we're missing or could improve. Treated as: research + implement the two concrete, well-defined gaps found; log everything else as a prioritized punch list rather than guessing at a site-wide redesign with no defined scope.

**1. Tawk.to live chat — DONE.** Confirmed on the live site (`embed.tawk.to` script, `Tawk_API` global) - real property ID `5dcaa5a1d96992700fc70d72`, `default` widget, no custom styling, so this is a straight port, not a design decision. Added the standard embed snippet as a `<Script strategy="lazyOnload">` in `app/layout.tsx`, right after the GTM/Clarity scripts (which stay placeholders pending real IDs - this one didn't need that, the ID came straight from the live site's own source). Verified locally: `Tawk_API` present, widget renders, chat form opens, and the same proactive "We Are Here!" greeting bubble shows (that's configured server-side on the Tawk dashboard against the property ID, so it came along for free).

**2. "Get a Quote" sticky side tab + panel — DONE.** The live site has a persistent vertical tab fixed to the right edge on every single page (not just some), opening a slide-in panel with a proper enquiry form: product-interest checkboxes (Security doors / Windows / Garage doors / Security shutters / Security grillers / Doors for trade), an enquiry textarea, and name/email/phone/postal address - a much lower-friction, higher-intent path to a quote than making someone navigate to `/contact-us`. This didn't exist anywhere in the migration. Built as `components/GetAQuote.tsx` (new global client component, mounted once in `layout.tsx` next to `Header`/`TestimonialsCarousel`/etc.), styled from the site's own design tokens (`--awb-color3` `#f5efe9` panel background, `--awb-color6` `#1c1e36` text, `--awb-color5` `#847b73` field underlines, Playfair Display heading) rather than guessed colors. Escape key and backdrop click both close it; body scroll locks while open. Submitting currently just shows a "Thank you" state and closes - it has no backend yet, same unfinished state as the main `/contact-us` form (see the standing "contact form backend" item below) - so this is a real UI/UX win now, wired to a real backend later in the same pass as the other form. Verified on both the default (dark-hero) and `isLightPage` header variants, and confirmed the checkboxes, Escape-to-close, and scroll-lock-release all actually work, not just render.

**Everything else found this pass — a prioritized punch list, not yet acted on:**

1. **Hero entrance animation (homepage, and likely other hero-carousel pages)** - the live site fades in the hero heading/CTA and cross-fades the background image on load/slide-change; our version currently just snaps to state. Real but purely cosmetic gap; worth doing once as a shared hero-carousel behavior rather than per-page.
2. **Minor container/gutter misalignment** - spotted on `/garage-doors` at identical viewport width (1425px): the live site's content column starts ~11px further right than ours (70px vs 59px left inset), consistent across the hero heading and the card grid below it. Same `fusion-builder-row` `104%`/negative-margin centering technique on both sides, so this is likely a small difference in an ancestor's computed width rather than a per-page bug - worth checking once, generically, rather than page-by-page.
3. **Contact form has no backend** (standing item, not new - see below) - the new Get-a-Quote panel above should get wired up in the same pass, both forms likely want the same backend (e.g. a `/api/contact` route emailing via Resend/SMTP, or a form service like Formspree) rather than two separate integrations.
4. Broader "150% / all pages" ask is too open-ended to act on blind - the right next step is picking specific page types or specific journeys (e.g. "the path from a category page to a quote," or "every profile-doors page side by side") to audit in depth, rather than a single unscoped sweep across 34+ pages.

`tsc --noEmit` clean. Not committed yet.

## Broken Avada "testimonials shortcode" carousel, sitewide — 2026-09-13 — FIXED

User reported specific paragraph text (from `/doors/profile-doors/fuego-fire`, the "Smoke protection doors (forster, janisol presto)..." paragraph) as "not working," and asked to find every other place the same thing happens before fixing anything.

**Root cause**: Avada's `.fusion-testimonials` widget (a text carousel, distinct from the trust-logo image marquee) renders all `.review` panels in the DOM but relies on its own JS to toggle which one has `.active-testimonial` (opacity 1 via the legacy fusion stylesheets; everything else is opacity 0 but still `display:block`, so it silently reserves layout space while being permanently invisible) and to move `.activeSlide` between the `.testimonial-pagination` dots on click/timer. That JS never runs in this migration (same category as the trust-logo marquee and the scroll-stack cards `ScrollReveal.tsx` already handles) - so only the first `.review` was ever visible, and the dots were dead `href="#"` links with no click handler. Confirmed via live `getComputedStyle` before touching anything.

**Sitewide survey** (`grep -rl "testimonials-shortcode-blockquote\|testimonial-pagination" app components`, excluding `app/legacy`) found 8 pages using this exact widget, all with the identical broken pattern:
- `components/doors/bullet-proof-doors/Row9.tsx` (4 reviews)
- `components/doors/panic-room-doors/Row17.tsx` (6 reviews)
- `components/doors/profile-doors/fuego-fire/Row4.tsx` (4 reviews) — the one the user quoted
- `components/doors/profile-doors/presto-bullet-proof/Row4.tsx` (2 reviews)
- `components/doors/profile-doors/stainless-steel/Row4.tsx` (2 reviews)
- `components/doors/profile-doors/unico-slim-line/Row4.tsx` (3 reviews)
- `components/garage-doors/side-hinged-garage-doors/Row9.tsx` (5 reviews)
- `components/grilles-shutters/security-shutters/Row10.tsx` (3 reviews)

**Fix**: one new global client component, `components/TestimonialsCarousel.tsx` (mounted once in `app/layout.tsx`, same pattern as `ScrollReveal.tsx`), rather than editing all 8 already-verified Row files. On mount (and on every route change, via `usePathname`), it finds every `.fusion-testimonials` widget on the page, reads its real `data-speed` attribute, and auto-advances which `.review`/dot pair carries `.active-testimonial`/`.activeSlide` on that timer; clicking a dot jumps straight to that slide and resets the timer. No Row files touched - none of the verified JSX changed.

**Real bug caught during verification, not assumed away**: the first version tracked the active index in a separate closure variable per widget. On the 6-review page (`panic-room-doors`) this desynced after a few ticks - the dot and the visible review pointed at *different* slides (e.g. dot 3 active while review 5 had the opacity). Rewrote so every tick/click derives the current index by reading which `.review` actually has `.active-testimonial` in the DOM right now, instead of trusting a counter - dot and review can no longer drift apart no matter how many times the effect fires. Also added a `data-testimonials-wired` guard on each widget so a Strict-Mode/Fast-Refresh double-invoke of the effect can't wire the same widget twice.

Verified live in-browser on `/doors/profile-doors/fuego-fire` (4 slides) and `/doors/panic-room-doors` (6 slides): polled review/dot active index every ~4s across multiple ticks and confirmed they always match and advance in order; dispatched clicks on multiple dots and confirmed both jump-to-slide and resume-autoplay work, indices staying in sync. `tsc --noEmit` clean.

Not committed yet.

## Header sticky behavior extended to isLightPage routes too — 2026-09-13 — DONE

Follow-up to the entry directly below: that first pass deliberately scoped the new dock/hide/reveal behavior to the default (dark-hero) header only, and left `/projects` + `/doors/profile-doors/*` (the "light page" routes - solid white bar, sits in normal document flow since there's no hero to float over) untouched. User asked for it across all pages including these.

The real risk here, not present in the default case: `isLightPage`'s header is normally `position: relative` and actually reserves layout space (unlike the default header, which was always absolute/floating and never took up flow space either way). Switching it to `position: fixed` on dock removes it from flow, which would jump all page content up by the header's own height the instant it docks. Handled with a spacer: measured the header's real rendered height via `ResizeObserver` (not a hardcoded guess - it differs between the desktop/medium row and the mobile row, which swap via CSS visibility at different breakpoints) and render an empty div of that exact height immediately before the header, only while both `isLightPage` and `stuck` are true. Also had to move the measurement `ref` from the outer class-carrying wrapper to `.fusion-tb-header` itself specifically - the spacer is a sibling *inside* that outer wrapper, so measuring the wrapper would have included the spacer's own height in the measurement, growing it on every resize/re-render.

CSS restructured so the docked (`.header-stuck`) rule is shared by both page types (same fixed positioning, background color swapped to white+border for light pages vs the translucent grey otherwise) instead of being duplicated per-branch, with the light-page "stay in relative flow at rest" rule scoped to apply only when *not* stuck (source-order tie-break against `.header-stuck`'s equal-specificity `!important` rule, same technique used for the footer alignment fix earlier this session).

Verified on both light-page routes (`/projects`, `/doors/profile-doors/fuego-fire`): dock, hide-on-scroll-down, and reveal-on-scroll-up all confirmed via DOM class checks after dispatched `scroll` events, and screenshotted to confirm zero layout jump - page content sits flush against the header both before and after it docks. Re-verified the default (dark-hero) homepage still works correctly after the CSS restructure (regression check, not assumed). `tsc --noEmit` clean, zero console errors on all three routes tested.

Not committed/pushed yet.

## Header: real sticky (dock + hide-on-scroll-down/reveal-on-scroll-up) — 2026-09-13 — DONE

Built the actual feature the "Sitewide header" investigation (below) stopped short of - this had been `position: absolute` at every scroll depth with zero scroll listeners since the original migration.

`components/Header.tsx`: added a `scroll` listener (passive, cleaned up on unmount) that tracks `window.scrollY` and its delta since the last event. Two pieces of state drive two CSS classes on the header's outer wrapper div:
- `header-stuck` once scrolled past 150px - docks both nav rows (`fusion-builder-row-1`/`-2`) as `position: fixed; top: 0` with the site's own sticky background color (`rgba(132,123,115,0.7)` - see note below on why this is hardcoded rather than read via `var()`).
- `header-hidden` when scrolling down by more than a 10px threshold while stuck (ignores small jitter from trackpads/momentum scrolling); cleared immediately on any scroll-up, and always cleared once back above the 150px stuck threshold.

**Real bug caught before it shipped**: the header's own inline style already carries `--awb-sticky-background-color: "rgba(132,123,115,0.7) !important"` - the obvious thing to do was read it via `background-color: var(--awb-sticky-background-color)`. That's invalid: a custom property's stored value is an opaque token stream, and when substituted through `var()` the literal text `!important` becomes part of the *value* (not a cascade origin flag) - `"rgba(...) !important"` isn't a valid `background-color` value, so the whole declaration is dropped and the header would render fully transparent once docked. Hardcoded the rgba value directly instead.

**Scoped deliberately to non-`isLightPage` routes only** (`/projects`, `/doors/profile-doors/*`) - those already solved a different problem (no hero to float over) by keeping the header in normal document flow with a solid white bar; turning that into a fixed docking/hiding bar too is a separate change with its own layout-shift considerations, not attempted this pass. Verified explicitly that `/projects` is completely unaffected by scrolling (no `header-stuck`/`header-hidden` classes ever applied, screenshot confirms the white bar renders exactly as before).

Verified the default (dark-hero) behavior for real, not just by reading the code: dispatched a genuine `scroll` event after `window.scrollTo` and confirmed via the DOM (not just visually) that `header-stuck`/`header-hidden` toggle correctly across scroll-down-fast, scroll-up, and back-to-top; screenshotted all three states. Also tried this session's `computer` tool's synthetic mouse-wheel scroll and `Page_Down` key action to test via a more "real" input path - **neither actually moved the page at all** (`window.scrollY` stayed `0`), and a temporary capture-phase listener confirmed zero `wheel`/`keydown` events ever reached the page - this is a limitation of synthetic input dispatch in this automated browser session (the same category of gap already documented for video autoplay and CSS animation timing in this environment), not a site bug: genuine user wheel/keyboard/touch scrolling fires real browser-native `scroll` events tied to `window.scrollY`, which is exactly what this implementation listens for. Flagging only so a future verification pass in a real, non-automated browser isn't skipped on the assumption this was already fully proven.

`tsc --noEmit` clean, zero console errors on fresh load and after scrolling.

Not committed/pushed yet.

## SecureCta testimonial video now autoplays — 2026-09-12 — FIXED

This is the `Secure-reviews-compressed.mp4` video flagged earlier this session as a maybe-intentional exception ("missing autoplay AND not muted... may be intentional click-to-play, not assumed to be the same bug"). User has now given explicit intent: it should autoplay. It already had `autoPlay={true}` in `components/home/SecureCta.tsx`, but no `muted` - browsers refuse to autoplay video with audio unless it's also muted, so the attribute was silently doing nothing. Added `muted={true}` (keeps `controls={true}`, so a visitor can still unmute/interact). Verified for real, not just by attribute presence: reloaded and read the live `<video>` element's own state - `paused: false`, `currentTime` climbing on repeated reads - genuinely playing, not stuck the way this environment's backgrounded-tab throttling affects other videos. `tsc --noEmit` clean.

Not committed/pushed yet.

## Trust-logo marquee: STILL reported broken by user despite two verified fixes — 2026-09-12 — deferred, needs fresh eyes

**Status: user says it's still doing the same thing after both fixes below. Deferred at their request ("let it be, will do that later on") - not resolved, do not assume either fix actually worked in their real browser.** Two rounds of work happened today, each mathematically/structurally verified from this side, neither confirmed fixed by the user:
1. Found and fixed a real cascade bug (`.swiper-wrapper { transform: none !important }` blocking the animation) plus a real pixel-width mismatch between the two duplicated logo sets (stale lazyload/srcSet attributes) - see git history same-day for detail.
2. Rebuilt the whole thing on a different, more robust technique (two independently-animated "collections" instead of one `translateX(-50%)` track - detail below, kept for reference) after the user linked a Stack Overflow answer for this exact class of problem and asked to implement it directly.

Both times, this session's own verification (computed styles, forced-keyframe pixel comparison, screenshots) said the fix was mathematically correct - and both times the user reported no change. That gap between "verified here" and "still broken there" is the important open question, not the CSS itself. Possibilities worth checking first next time, before writing more marquee CSS: (a) the user may be looking at a cached page (hasn't hard-refreshed / dev server hot-reload didn't pick up the layout.tsx change - this file's inline `<style>` block is large and has caused confusion before); (b) the user may be looking at the deployed Vercel production site rather than `localhost:3000` - nothing from today has been pushed; (c) there may be a genuinely different bug this session hasn't found (worth asking the user for a screen recording or a precise description of what they see - "jumps," "pauses," "shows a blank gap" are all different failure modes with different causes, and this session has been guessing at which one from a text description each time).

### Two-collection technique detail (kept for reference, not proven to have fixed the user's actual complaint)

User found a reference solution (stackoverflow.com/a/65485329, Albert Fernández Martínez, CC BY-SA 4.0) for this exact class of "seamless infinite marquee" problem and asked to implement that pattern directly, rather than continue patching the previous single-track `translateX(-50%)` approach.

Restructured `components/home/TrustLogos.tsx`: the 8 slides (4 real logos + 4 duplicates) are now grouped into two wrapper divs, `.item-collection-1` and `.item-collection-2` (adapted from the source's `<span>` wrappers to `<div>`, since our slides are block-level - spec-invalid to nest divs inside spans). New CSS in `app/layout.tsx`: the outer `.awb-image-carousel-wrapper` track slides via `translateX(0 → -100%)` over 22s exactly as before, but now `.item-collection-1` *also* animates independently - `left: 0% → 100%` at the animation's exact halfway point (`50%, 50.01%` keyframe split, a discrete jump not a transition) - timed so the jump exactly cancels out the distance the track has already slid, making collection-1 reappear seamlessly right where collection-2 was.

Verified the seam is mathematically exact, not just "looks fine": paused both animations, forced the wrapper to its exact 50%-keyframe transform and collection-1 to its exact post-jump `left` value via JS, and screenshotted - the resulting frame is pixel-identical to the animation's starting frame (t=0), proving there is no visible discontinuity at the loop boundary *in this environment*. `tsc --noEmit` clean, zero console errors on reload. **This verification clearly wasn't sufficient to catch whatever the user is actually seeing** - flagging that explicitly rather than re-asserting it's fixed.

Not committed/pushed yet.

User found a reference solution (stackoverflow.com/a/65485329, Albert Fernández Martínez, CC BY-SA 4.0) for exactly this class of "seamless infinite marquee" problem and asked to implement that pattern directly, rather than continue patching the previous single-track `translateX(-50%)` approach (which had already needed one real fix earlier today - a lazyload/srcSet width mismatch between the two duplicated logo sets - and remained fragile to any future width mismatch between the two halves by construction).

Restructured `components/home/TrustLogos.tsx`: the 8 slides (4 real logos + 4 duplicates) are now grouped into two wrapper divs, `.item-collection-1` and `.item-collection-2` (adapted from the source's `<span>` wrappers to `<div>`, since our slides are block-level - spec-invalid to nest divs inside spans). New CSS in `app/layout.tsx`: the outer `.awb-image-carousel-wrapper` track slides via `translateX(0 → -100%)` over 22s exactly as before, but now `.item-collection-1` *also* animates independently - `left: 0% → 100%` at the animation's exact halfway point (`50%, 50.01%` keyframe split, a discrete jump not a transition) - timed so the jump exactly cancels out the distance the track has already slid, making collection-1 reappear seamlessly right where collection-2 was. This technique doesn't depend on the two collections being pixel-identical widths the way the old approach did.

Verified the seam is mathematically exact, not just "looks fine": paused both animations, forced the wrapper to its exact 50%-keyframe transform and collection-1 to its exact post-jump `left` value via JS, and screenshotted - the resulting frame is **pixel-identical** to the animation's starting frame (t=0), proving there is no visible discontinuity at the loop boundary, confirmed by rendered comparison rather than assumed from the CSS alone. `tsc --noEmit` clean, zero console errors on reload.

Not committed/pushed yet.

## Sitewide section split — all 34 remaining pages — 2026-09-12 — DONE

Following the homepage pilot (below), user asked to do the same for every page, not just one. Generalized the one-off homepage script into `scripts/split_page_sections.js`, which auto-detects each page's `fusion-builder-row-N` siblings (via the same `<div>`/`</div>` depth-counting technique, extended to auto-detect boundaries rather than hardcoded line numbers) and extracts each into `components/<page-slug>/RowN.tsx`.

Ran it across all 37 page.tsx files (excluding `app/legacy/*`): 34 processed, 3 correctly skipped (`/blog`, `/blog/[slug]`, `/thank-you` - none use the fusion-row pattern, already small hand-built pages with nothing to gain from splitting).

**First run had a real bug, caught before it did damage.** The initial version counted div-depth starting from each row's *own* opening tag; several pages (`communal-entrance-doors`, `tracless-garage-doors`, `colllabsible-grilles`, `grilles-shutters` hub, `security-aluminium-windows`) wrap a row in an extra anchor div (`fusion-container-anchor`, used for in-page nav scroll targets) that opens *before* the row's own marker - counting from the wrong point meant that wrapper's closing tag was never matched, corrupting every subsequent section in that file. `tsc --noEmit` caught this immediately (dozens of "no corresponding closing tag" errors) before anything was committed - reverted the entire batch (`git checkout` on all 34 files + delete the new component dirs) rather than trying to hand-patch broken output. Root-caused and fixed the script itself (count depth from the actual section start, which already includes any such wrapper, instead of from the row's own tag), plus a second related fix (some rows are themselves nested widgets containing further row-numbered markers as children - e.g. a scroll-stack card's individual slides - which aren't separate top-level siblings and need to be swallowed whole rather than processed again as their own section). Re-ran clean: all 34 processed, `tsc --noEmit` clean across the whole project on the first attempt after the fix.

Every file's extraction is verified individually, not just trusted: the script sorts every line from the original section range against every line from the newly-written component files and refuses to write anything for a page unless they match exactly - this is what caught the depth-counting bug in the first place (well, `tsc` caught the symptom; the sorted-diff check is what would have caught quieter, non-compiling-but-wrong corruption too, and confirms losslessness for all 34 on the successful run). Spot-verified visually and via HTTP 200 checks across a sample spanning the size range (the 7249-line `doors/high-security-doors` down to 306-line `windows`), zero console errors, zero regressions.

### Bonus find while spot-checking: 3 category-hub pages missing real navigation links — FIXED

While clicking through post-split pages, user found `/grilles-shutters`'s 3 category cards ("Security shutters", "High security shutters", "Collabsible grilles") had no links at all - confirmed this is a **pre-existing bug predating today's session entirely** (checked `git show HEAD~1`, the commit before any of today's work touched this file - already missing there), not something the split introduced. Root cause: Avada's "liftup" hover-card widget (`fusion-column-liftup-border`) wraps its background-image in a real `<a class="fusion-column-anchor" href="...">` on the live site, and our scrape/conversion dropped that anchor for some pages while keeping it for others. Checked every page using this same card widget: `/doors` and `/products` already had the correct links (untouched); `/grilles-shutters`, `/garage-doors`, and `/windows` did not. Fixed all three to match the working pattern, sourcing the exact target URLs from the live site's own `fusion-column-anchor` hrefs (not guessed) and confirming card-title order matched exactly before wiring each one up:
- `/grilles-shutters`: Security shutters → `/grilles-shutters/security-shutters/`, High security shutters → `/grilles-shutters/high-security-shutters/`, Collabsible grilles → `/grilles-shutters/colllabsible-grilles/`.
- `/garage-doors`: Sectional → `/garage-doors/sectional-garage-doors/`, Tracless → `/garage-doors/tracless-garage-doors/`, Side hinged → `/garage-doors/side-hinged-garage-doors/`, Sliding → `/garage-doors/sliding-garage-doors/`.
- `/windows`: High security steel windows → `/windows/high-security-steel-windows/`, Security aluminium windows → `/windows/security-aluminium-windows/`.

Verified by reading each card's actual `href` back from the live DOM after the fix (not just eyeballing the JSX) and by clicking one through end-to-end (`/grilles-shutters` → "Security shutters" card → landed on `/grilles-shutters/security-shutters` as expected). `tsc --noEmit` clean.

Not committed/pushed yet.

### DONE 2026-09-13: rename `RowN.tsx` files to meaningful names

Superseded by the "RowN.tsx renamed to meaningful names sitewide" entry near the top of this file - user asked for the full sweep rather than the incremental plan below, and it turned out to be safe to do well: piloted first, reviewed the full output before committing, caught and fixed 4 real bugs (heading-truncation quality issues plus digit-prefixed invalid identifiers).

The homepage split (below) named each file for what it actually is (`CategoryCards`, `TrustLogos`, `SecureCta`, etc.) because each was read and named individually. The sitewide split across the other 34 pages used generic `RowN` names instead (accurate to the source Avada markup, but tell you nothing about the content without opening the file) - purely because it ran as one mechanical script across ~250 sections, not something safe to guess names for automatically without actually reading each one (a wrong guess, e.g. calling a testimonials block "IntroText", would be worse than the current honest-but-unhelpful `RowN`). User agrees this is worth doing but wants it done properly, not rushed. Plan: rename incrementally, same as the "split it when you're touching it anyway" approach already used for the section split itself - whenever either of us opens a given `RowN.tsx` for any reason, rename it and its import in the parent `page.tsx` at the same time, rather than one big rename-everything pass. Left as a standing TODO here rather than done today.

## Homepage split into section components — 2026-09-12 — DONE (pilot, one page)

User asked (after this session's repeated back-and-forth fixing individual homepage sections - marquee, videos, footer, hero) to split pages into per-section components for maintainability, as a pilot on one file before deciding whether to do the rest. Picked the homepage since it's the one we'd been elbow-deep in all session.

`app/page.tsx` was 1310 lines with all 9 content sections (`fusion-builder-row-4` through `-12`) inlined directly in the JSX, on top of the header/wrapper scaffold - exactly why the marquee/video fixes earlier this session each required scrolling through hundreds of lines to find the right block. Extracted each section into its own file under `components/home/`: `IntroHeading`, `CategoryCards`, `LuxuryDoorsDetails`, `DoorsDesignVideo`, `BespokeManufacturer`, `TrustedManufacturer`, `OurProjects`, `SecureCta`, `TrustLogos` - each a plain no-props function component (all content was already static JSX, nothing dynamic to thread through). `app/page.tsx` is now 82 lines: imports, metadata, the wrapper scaffold, and the 9 components rendered in their original order.

Did the extraction via a script (`scripts/split_homepage.js`) rather than manual cut-and-paste, matching each section's boundary by depth-counting `<div>`/`</div>` tags from its `fusion-builder-row-N` opening line to the balanced close (same technique proven correct for the earlier logo-strip reorder this session) - far less error-prone than manually eyeballing indentation across a 1300-line file. Verified the split preserved content exactly, not just "looked right": extracted every line from the original file's sections 54-1299 and every line from the 9 new files' bodies, sorted both, and confirmed the two lists are identical line-for-line (1246 lines each) - proof nothing was dropped, duplicated, or altered in the move, only relocated. `tsc --noEmit` clean, zero console errors, and a full-page screenshot pass confirmed the homepage renders pixel-identical to before, in the same order, marquee still animating.

**Not done**: the other 34 pages. Treating this as validated but intentionally not rolled out everywhere yet, per the plan discussed with the user - extract a page's sections when next touching it for another reason, rather than a big-bang pass across the whole site that touches every page for no immediate benefit.

Not committed/pushed yet.

## Sitewide header: real sticky behavior — investigated, NOT implemented — 2026-09-12

User asked how the header works on the live site (color seems to invert light/dark, it hides on scroll-down and reappears on scroll-up, "some places it has a colored bg too") and separately noticed a "white strip" at the top of some pages on our site. Investigated the live site directly rather than guessing:
- **The color-inversion is an illusion, not an active feature.** The header row is `position: fixed; top: 0` with a CONSTANT `rgba(132, 123, 115, 0.7)` background (confirmed identical at every scroll position tested) and CONSTANT white text. It only *looks* like it changes color because a 70%-opaque warm-grey bar composites differently depending on what's scrolling underneath it (white section → looks light/washed; dark hero image → looks dark). Nothing to "replicate" here beyond implementing the fixed positioning + translucent background correctly - the apparent color adaptation falls out of that automatically.
- **The hide-on-scroll-down/show-on-scroll-up is real and JS-driven.** Confirmed via DOM inspection: Avada's real (still-running, on the live site) scroll listener toggles a `fusion-scrolling-down` class on the sticky row, which its compiled CSS uses to slide it up/hide via transform + opacity. This is exactly the already-logged "Header confirmed genuinely non-sticky" finding from the CEO pre-launch review (see below) - `Header.tsx` on our site never got this reimplemented; it stays `position: absolute` at every scroll depth with no scroll listener at all. **This needs a real implementation** (a scroll-direction-aware fixed header, hide/show transition, translucent background using the `--awb-sticky-background-color` variable already present in `Header.tsx`) - out of scope to improvise inside this session's already-large batch of fixes; flagging as the next well-defined, standalone piece of work.
- **Found the actual "white strip" cause while investigating, and it's more specific than a generic gap**: on `/blog` specifically, the header renders with fully transparent background and white text (confirmed via computed style: `rowBg: rgba(255,255,255,0)`, `linkColor: rgb(255,255,255)`) - invisible against `/blog`'s plain white top section, since (unlike every other page) there's no dark hero image underneath to give the white text contrast. This reads as "a blank white strip where the header should be," not a missing element - the header is there and clickable, just invisible. **This will very likely resolve itself once the real sticky header above is implemented**, since a properly-implemented sticky header carries a real (non-fully-transparent) background rather than relying on a hero image for contrast. Did not attempt a narrower one-off fix for `/blog` alone (e.g. wrapping it in the full Avada body-class list other pages carry) - that's a more invasive structural change to a page that's already been fixed twice this session for related-but-distinct reasons (see below), and risks side effects without more testing than this session has budget for.

## `/blog` page: two more real bugs found and fixed — 2026-09-12 — FIXED

Continuing to investigate the header issue above surfaced that `/blog` - the one hand-built page in the site (not scraped from Avada, uses its own `page.module.css`, no per-page `<link rel="stylesheet">` at all) - was missing far more than the one image-overflow bug found and fixed earlier this session (see "Sitewide mobile-overflow sweep" below).

- **The shared `<Header/>`/`<Footer/>` components were rendering almost completely unstyled on `/blog`** - the desktop menu showed as a raw bullet list, the logo area was blank, and the footer had zero of its layout CSS (columns un-aligned, text overlapping the content above it). Root cause: every Avada-scraped page loads its own compiled `fusion-styles/[hash].min.css` bundle, and - despite the per-page hash suggesting each is unique - these bundles carry a large shared "base theme" CSS payload (menu layout, image resets, etc.) that the shared Header/Footer components depend on, in addition to genuinely page-specific rules. Confirmed by comparing stylesheet rule counts: `/blog` loaded ~100 CSS rules total versus several thousand on every other page. `/blog` never had any such link at all. **Fix**: added a `<link rel="stylesheet" href="/legacy-assets/uploads/fusion-styles/3978f22170001630860f0711fbc80184.min.css" />` (the homepage's bundle) to `app/blog/page.tsx`. Verified: footer now renders with fully correct layout/alignment matching every other page. This is the same underlying gap that caused the `.video-wrapper` unclamped-image bug fixed earlier - both are consequences of `/blog` never loading a bundle every other page happens to depend on.
- **Consequence of the above fix, not a new regression**: with real menu/header CSS now loading, the header's *content* renders correctly, but its white-on-transparent styling is invisible against `/blog`'s white background (see the header investigation above) - this was already broken before the stylesheet fix too (previously it was ugly/unstyled-but-visible black text; now it's correctly-styled-but-invisible white text). Net readability is arguably about the same today; the real fix for this is the sticky-header work above, not a `/blog`-specific patch.
- `tsc --noEmit` clean after the stylesheet addition.

## Homepage trust-logo marquee: fixed two real bugs (frozen + visible seam) — 2026-09-12 — FIXED

Continuing from the earlier "made it rollable" fix (see the mobile-overflow sweep entry below) - user reported it was still not moving in their own real browser, and then, after the true fix, that it moves but visibly jumps/gaps once per loop instead of scrolling seamlessly. Both were real, found and fixed for real (not automation-environment artifacts, despite this session's earlier well-founded caution about that class of false negative):

1. **Root cause of "still not moving": an unrelated earlier rule was silently overriding the marquee's animation.** `app/layout.tsx` has a long-standing rule, `.swiper-wrapper { transform: none !important; ... }` (originally written to force Avada's "Our Projects" Swiper carousel into a static grid), which also matches the trust-logo marquee's own wrapper (it carries both `swiper-wrapper` and `awb-image-carousel-wrapper` classes). Per the CSS Animations spec, an `!important` declaration for a property beats a running keyframe animation trying to change that same property, even though the animation's own `animationPlayState` correctly reports `"running"` the entire time - this is exactly what made the bug look like an environment/automation artifact at first (computed `transform` stuck at `"none"` despite a technically-running animation), when it was actually a real, fully reproducible cascade conflict. **Fix**: scoped the offending rule to `.swiper-wrapper:not(.awb-image-carousel-wrapper)` so it no longer touches the marquee at all. Verified via `element.getComputedStyle().transform` changing from a literal `"none"` to a real matrix value after the fix (confirming the block was lifted) - full frame-by-frame visual confirmation of movement still isn't obtainable from this specific automated tab (Chrome throttles the animation's timeline to a full stop for a backgrounded/unfocused tab, the same previously-documented limitation that also affects `<video>` autoplay here), but the actual blocking mechanism is gone and the fix is now provably correct in a real, focused browser.
2. **Root cause of "visible gap/jump once per loop": the two duplicated logo sets weren't pixel-identical widths.** The marquee works by duplicating the 4 logos once and animating `translateX(-50%)`, which is only seamless if both halves are exactly the same width. Two of the four logo `<img>` tags (`Secure-ikonos-09-2.png`, `Group-152.png`) still had their original WordPress `lazyload`/`srcSet` attributes on the *first* copy but a plain fixed `src` on the *duplicate* copy - since the responsive `srcSet` could resolve to a different candidate image width than the duplicate's fixed one, the two "halves" of the marquee track weren't always the same total width, so the halfway animation point didn't land exactly at the loop seam. **Fix**: stripped the leftover `lazyload`/`data-orig-*`/`srcSet` attributes from both original images in `app/page.tsx`, making all 8 slides (4 real + 4 duplicate) deterministically identical. Verified: measured all 8 slide widths after the fix - first half `[183, 296, 303, 205]` now matches the second half exactly, element-for-element.

`tsc --noEmit` clean after both fixes.

## Homepage video crop bug: 2 videos fixed — 2026-09-12 — FIXED

User reported a video near the "Bespoke Manufacturer" section (in the "Luxury Security Doors DETAILS" two-column block) was showing different cropped portions on different screens - "on my big screen half the video is cropped, on some other screen even the bag in the video is visible." Root cause: the video's column sits in a two-column flex row (video + heading text), and Avada's flex row stretches both columns to match the taller sibling's height - which changes with viewport width as the heading text wraps differently. The video (a *portrait* 746×1000 clip, confirmed via `ffprobe`) had `object-fit: cover` with no explicit height, so it always filled whatever height the stretch produced, cropping a different vertical slice of the portrait footage depending on how tall that happened to be at a given screen width - exactly matching the reported symptom. A second video sharing the identical bug (`Bullet-proof-1.trumpesnis-2-2.mp4`, also 746×1000, same two-column layout pattern, near "Explore protection levels") was found and fixed the same way, unprompted, since leaving it broken would have been inconsistent.
**Fix**: set `aspect-ratio: 746 / 1000` (the video's real, `ffprobe`-measured ratio) plus `object-fit: contain` on both `<video>` elements in `app/page.tsx`, so the box height is now derived deterministically from the known-correct aspect ratio regardless of any flex-stretch side effects, and `contain` guarantees the full frame is visible even in the unlikely case the box doesn't land exactly on that ratio. Verified visually at three very different viewport widths (390px mobile, 1568px desktop, 2560px ultra-wide) - full, uncropped, correctly-proportioned video frame at every size, confirmed via measured aspect ratio matching 0.746 exactly. `tsc --noEmit` clean.

## Homepage footer: real alignment bug found and fixed — 2026-09-12 — FIXED

User asked to "rearrange the footer, make it look good, fix all the alignment." Checked against the live site first rather than guessing: confirmed the footer's 5-column proportions and desktop layout already match the live site's own design intent (the extra "Our Services" column is a previously-and-deliberately-added feature, not a bug - see `PROGRESS.md`). But found one real, unambiguous bug along the way: **at ≤1024px (confirmed exact breakpoint), all 5 footer columns collapse to one full-width stacked column, but only the logo column was ever centered - the phone/email/address text and all 3 nav menus (Quick Links, Our Services, About Us/Terms) kept their desktop left-aligned flex-start positioning**, producing a lopsided look (centered logo sitting above text and links all hugging the left edge). Confirmed this exact same lopsided pattern exists on the live WordPress site too at the same width - so it's not a migration-introduced defect - but since the user explicitly asked for this to "look good" rather than just match parity, fixed it as a real improvement rather than leaving it as-is: added a `@media (max-width: 1024px)` rule in `app/layout.tsx` that centers every footer column's content and text once stacked. Two things made this non-trivial: (1) some column wrappers use `flex-direction: row` and others `column`, so both `align-items` and `justify-content` had to be forced together to cover whichever axis is the centering axis for a given column; (2) the two Avada `awb-menu` components (Quick Links, About Us/Terms) read their alignment off a `--awb-main-justify-content` CSS custom property set inline in `Footer.tsx` rather than a plain class, so a generic `text-align`/`align-items` override never reached them - had to override that specific variable plus `justify-content` directly on their `.awb-menu__main-ul`. Verified at 1024px, 390px (mobile), and 1568px (desktop, confirming the desktop layout is completely untouched by the new media-query-scoped rule) - every column now centers consistently as one column at every stacked width, with zero effect above 1024px. `tsc --noEmit` clean.

## Hero slider: removed forced auto-advance timer — 2026-09-12 — DONE

User asked to remove the hero slider's automatic slide-rotation timer so it only advances on click (the dot navigation). `components/HeroSlider.tsx` had a `setInterval`-driven `useEffect` rotating slides every 6.5s (`ROTATE_MS`); removed that effect entirely. The existing click handlers on the dot navigation (`onClick={() => setActive(i)}`) were already independent of the timer, so slide-switching by click continues to work exactly as before with no other changes needed. `tsc --noEmit` clean.

## Homepage trust-logo strip repositioned — 2026-09-12 — DONE

User asked to move the homepage trust-logo strip (the "10 Year Warranty" / Google 4.8 / "Trade Specific 20 Years" / Offsite Awards marquee, `fusion-builder-row-11` in `app/page.tsx`) to sit below the "Let's secure your property Together" photo+video CTA section (`fusion-builder-row-12` — one column has a `Mask-group-10.png` background photo, the other a testimonial video) and directly above the footer, matching the live WordPress site's order. It was previously positioned *before* that CTA section instead of after it.

Moved the entire `fusion-builder-row-11` block (228 lines) to after `fusion-builder-row-12` in `app/page.tsx`, so the order is now: Our Projects → photo+video CTA → logo strip → `<Footer/>`. Did the move via a line-range `sed` reconstruction rather than a manual JSX edit (the block is too large and repetitive for a reliable string-match edit) — verified before writing it back that the reordered file was a byte-for-byte **pure permutation** of the original (sorted-line diff empty after correcting a CRLF-vs-LF line-ending artifact `sed` introduced), so no content was gained, lost, or altered, only reordered. `tsc --noEmit` clean afterward. Verified visually on both desktop and mobile (390px) screenshots: strip now renders directly beneath the photo/video section and immediately above the footer, no overflow introduced, `git diff --stat` shows exactly the expected 83-line block moved (not a whole-file rewrite).

**On "make it rollable" — investigated, did not find a code bug, flagging the limits of what could be verified here.** The marquee is not a placeholder; `app/layout.tsx` already has a real CSS-only infinite-scroll implementation (`.awb-image-carousel-wrapper { animation: awb-logo-marquee 22s linear infinite }`, with the 4 logos duplicated once in the JSX so the loop is seamless, and `overflow: hidden` correctly set on the parent to clip it into a window). Checked via the Web Animations API (`element.getAnimations()`) that the animation is attached and its `playState` reports `"running"`, with correct duration and keyframes. However, this automated browser session could not get the timeline to actually visibly advance (`animation.currentTime` stayed frozen at `0` even while `playState` said "running") — this traces to `document.visibilityState === "hidden"` / `document.hasFocus() === false` in this specific environment, the exact same previously-documented limitation that also suspends `<video>` autoplay in this session (see the "Homepage video not showing" entry elsewhere in this log). Because of that, this could not be independently confirmed as visually scrolling in a real, focused browser tab from this side. **If it's still static after this session's changes reach a real browser, that's a genuine follow-up bug to hunt down** — the code looks structurally correct, but "looks correct" isn't the same as "confirmed working," and this is exactly the kind of thing that's easy to misjudge from an automated environment with this specific limitation.

Not committed/pushed yet.

## Sitewide mobile-overflow sweep — 2026-09-12 — FIXED (1 real bug), rest already clean

Continued the mobile responsiveness batch (Home/About Us/Projects/Products, then a product-detail page and Contact Us — all previously confirmed clean) by extending the same real-viewport overflow scan to every one of the 36 sitemap routes, rather than assuming the pattern held. Used a scripted pass (390px iframe viewport, scan every element for `getBoundingClientRect().width` exceeding the viewport) instead of manual screenshotting for the remaining pages, since 6 manual pages across every content archetype had already found nothing — this made it possible to actually check all 36 in one sitting instead of guessing the rest were fine.

**Found and fixed one real bug: `/blog`.** `scrollWidth` was 548px against a 390px viewport — a genuine horizontal-scroll bug, not a false positive. Root cause: `Header.tsx` renders a fixed `width="495"` watermark logo `<img>` (`wp-image-1903`) with no CSS clamping it down on its own. On every one of the 35 Avada-scraped pages, that page's own compiled per-page stylesheet (`fusion-styles/[hash].min.css`, or `homepage-styles.css` on the homepage) happens to carry a plain `img { max-width: 100%; height: auto; }` reset, which is what actually keeps this image responsive — confirmed by checking the image's rendered width dropped from 495px to 168px on the homepage specifically because of that page's stylesheet, not anything in `Header.tsx` itself. `/blog` is the one hand-built page (not scraped, own `page.module.css`, no per-page Avada stylesheet), so nothing clamped it there and the raw 495px width overflowed the 390px viewport. **Fix**: moved that reset into `app/theme-globals.css` (imported globally in `app/layout.tsx`, so it loads on every page including `/blog`) rather than relying on any single page's own stylesheet — the shared `<Header/>` component shouldn't depend on which page happens to load a legacy per-page CSS bundle. Verified: `/blog`'s `scrollWidth` is now 390 (exact match, zero overflow) and the watermark renders at a correctly clamped 330px. Re-ran the 6 already-tested pages afterward to confirm the global CSS addition caused zero regressions (all still clean) before moving on. `tsc --noEmit` clean.

**All other 35 routes: zero horizontal overflow, no fix needed.** Full list checked: `/`, `/about-us`, `/blog` (fixed), `/contact-us`, `/door-styles` + its 4 style sub-pages, `/doors` + all 8 of its sub-pages (bullet-proof, communal-entrance, fire-resistant, high-security, panic-room, profile-doors + its 4 product variants), `/garage-doors` + its 4 sub-pages, `/grilles-shutters` + its 3 sub-pages, `/products`, `/projects`, `/security-levels`, `/trade`, `/windows` + its 2 sub-pages. The one home-page carousel track flagged by the scan (1923px wide) is confirmed-safe — clipped correctly by its parent's `overflow-x: hidden`, normal carousel behavior.

**Scope note, so this isn't overstated**: this sweep is a horizontal-overflow check, not a full per-page visual/typography audit. 6 pages (Home, About Us, Projects, Products, `/doors/bullet-proof-doors`, Contact Us) got the deeper treatment — full-scroll screenshots, FAQ-accordion click test, form input font-size/touch-target check, mobile-menu click test. The other 29 only got the scripted overflow scan. Given the very consistent "already clean" result across every one of the 5 content archetypes in the deep-checked sample, and the site-wide shared CSS system, this is a reasonable confidence level — but if a specific page turns up a visual (not overflow) problem later, that's exactly the kind of thing this lighter pass wouldn't have caught.

Not committed/pushed yet.

User confirmed the verified Antigravity audit (see review below) is worth acting on, but deferred it — "will do that later on." Full detail and exact file paths live in `migration-log/RECOMMENDED-IMPROVEMENTS.md`; short list here so it isn't lost:
- [ ] Remove/relocate `app/legacy/` (71 files, 35 dead duplicate routes) — cuts build + typecheck time roughly in half. Also remove its `/legacy` exclusions from `app/robots.ts`/`app/sitemap.ts` once gone.
- [ ] Untrack `scratch/` from git (364MB, not in `.gitignore`) — add to `.gitignore`, `git rm -r --cached scratch/`, move the original video archives out of the repo.
- [ ] Gate the GTM/Clarity dummy IDs in `app/layout.tsx` behind an env var (`NEXT_PUBLIC_GTM_ID` etc.) rather than shipping `GTM-XXXXXXX`/`CLARITY-XXXXXXX` as literal script calls on every page — note these already have `// TODO: ... from Priyanka` comments, so this is finishing a known pending item, not discovering dead code.
- [ ] Purge/archive 221 unreferenced upload files (71.26MB) plus the 3 corrupt `.heic` files and 2 `.png,` (literal trailing comma) files.
- [ ] Move `cheerio` from `dependencies` to `devDependencies` in `package.json` (only used in offline migration scripts).
- [ ] Remove the dead Easy-Digital-Downloads CSS `<link>` tags shipped on every page (34 files).
- [ ] Delete stray root-level scrape dumps: `all url.txt`, `chat.txt`.
- [ ] Add `<title>`/`<meta description>` (`Metadata` export) to `app/door-styles/french-doors/page.tsx` and `app/door-styles/edwardian-doors/page.tsx` — currently missing entirely.
- [ ] Everything already in this file's own sections (contact form backend, real sticky header, door-styles dead links, security headers, redirects, profile-door PDFs) is the same backlog, already tracked above/below — not duplicated again here.

## Third-party audit review (Antigravity) — 2026-09-12 — kept, not deleted

User had another AI tool ("Antigravity") independently scan the codebase and asked whether the output (`IMPROVEMENTS.md` at repo root, `migration-log/RECOMMENDED-IMPROVEMENTS.md`, `migration-log/MOBILE-RESPONSIVE-SPEC.md`) was worth keeping or should be deleted. Spot-checked every concrete, checkable claim in it against the actual codebase rather than trusting it at face value:
- Confirmed exactly right: `app/legacy/` really is 71 files / 35 dead routes; the GTM/Clarity dummy IDs are real (though note they already carry their own `// TODO: replace with real ... ID from Priyanka` comments in `layout.tsx` — this is a known pending item waiting on a stakeholder, not accidental dead code, so "just remove it" isn't quite right, gating behind an env var is the better version of their suggestion); `scratch/` is really 364MB and tracked in git (147 files); the 3 `.heic` and 2 `.png,` files exist at the exact paths given; `cheerio` really is misplaced under `dependencies`; `next.config.ts` really is empty (no headers/redirects); `/door-styles`'s 4 style cards really have zero working links to child pages (confirmed independently — matches our own already-logged finding below); French/Edwardian doors really lack `metadata` exports.
- Several findings usefully corroborate things already independently found and logged in this file (contact form has no backend, header isn't really sticky, door-styles links are dead, profile-door PDFs missing, no security headers, no redirects) — good cross-validation, not new information, but a healthy sign the rest of the document is trustworthy.
- **Verdict: kept, all 3 files.** This is a solid, verified audit, not slop. Not merged into this file's own structure (it's organized well enough on its own) — treat `RECOMMENDED-IMPROVEMENTS.md` as the dead-code/launch-blocker backlog going forward, and `MOBILE-RESPONSIVE-SPEC.md` as a reference (not a drop-in) for the mobile responsiveness work on the remaining ~32 pages — see below for why it wasn't applied wholesale.

## Home / About Us / Projects / Products mobile responsiveness — 2026-09-12 — checked, no fix needed

User asked to make Home, About Us, Projects, and (added in a follow-up) Products "proper mobile responsive" as the first archetype-representative batch, planning to carry forward whatever's learned into the rest of the site in later batches. Before writing any CSS, tested all 4 pages for real: injected a genuine 390×844 iframe viewport (the established workaround — `resize_window` doesn't actually change `window.innerWidth` in this environment) and ran a script scanning every element for horizontal overflow, then screenshotted the full scroll depth of each page.

**Result: all 4 pages are already clean.** Zero horizontal-overflow elements found on any of them (`document.documentElement.scrollWidth` ≤ 390 on all four; the one wide element found on the homepage, a 1923px-wide image-carousel track, is correctly clipped by its parent's `overflow-x: hidden` — normal carousel behavior, not a bug). Visually scrolled the full height of all 4 pages at mobile width: columns/cards stack cleanly to 1-per-row (including Products' 4-category tab bar and its per-category card grids — the archetype most likely to break, per Antigravity's own "Category Hub" pattern, and it didn't), headings and body text are readable at sensible sizes, footer stacks and centers correctly, and the mobile hamburger menu opens correctly (spot-checked by actually clicking it on `/projects`, which uses a client-component wrapper unlike the others — full-screen panel opened correctly, zero console errors).

**No shared "fix pattern" to carry forward yet** — there was nothing broken to fix on any of these 4, so there's no CSS rule discovered here that the next batch needs. What *does* carry forward: the testing method itself (real iframe viewport + overflow scan + full-scroll screenshot pass) and the now-3x-confirmed fact that this codebase's existing flex/CSS-variable column system already handles simple content and card-grid archetypes correctly without extra mobile CSS. The genuinely different archetypes still untested — product detail pages with spec tables/accordions, and the contact form — are where Antigravity's `MOBILE-RESPONSIVE-SPEC.md` patterns (table scroll, 16px form inputs to block iOS zoom, etc.) are actually likely to be needed; that's the next thing worth testing for real rather than assuming.

This is a direct result of this session's earlier mobile-menu and hero-heading fixes (both applied globally in `app/layout.tsx`/`Header.tsx`, so they already cover every page) plus these 3 pages' content being simple text/image/card blocks — the more failure-prone patterns Antigravity's spec describes (wide spec tables, multi-column forms, dense product grids) don't appear on these 3 pages. **Did not apply Antigravity's `MOBILE-RESPONSIVE-SPEC.md` wholesale** — it's a reasonable reference for patterns to watch for on other pages, but applying its blanket sitewide CSS engine here would have been fixing a problem that doesn't exist on these 3 pages, and risks regressing pages not yet visually verified (against this project's own rule: pilot and verify before batch-applying). Genuinely tables/forms/dense-grid pages (product detail pages, contact-us) are exactly where that spec will earn its keep — save it for those batches.

## TODO (not started) — Lighthouse audit across all pages + report log

User asked (2026-09-11) to run a Lighthouse check across every page and produce a report log — deferred for later, noting it here so it isn't lost. Not started yet: no Lighthouse run has been done this session, nothing installed/configured for it yet. When picked up: check whether `lighthouse`/`@lhci/cli` needs adding as a dev dependency, run it against all 36 routes on the local dev server (or a Vercel preview, TBD), and write the results into a new dated report file under `migration-log/` (perf/accessibility/best-practices/SEO scores per page, flag anything below a reasonable threshold) rather than just pasting raw output here.

## Follow-up image audit — same-filename-different-folder bug class — 2026-09-11 — FIXED

User manually spot-checked a handful of pages after the previous round of image fixes and reported 4 more visually-wrong images (on pages the original `compare_all_images.js` had marked PASS). Root cause: that script compared images by **basename only** (`Mask-group-26.png`), so when WordPress has the same filename re-uploaded into two different month folders with genuinely different content (`/2025/01/Mask-group-26.png` vs `/2025/03/Mask-group-26.png`), the script saw a filename match and reported no problem even though our page pointed at the wrong folder's file.

Wrote `scripts/compare_all_images_strict.js` — same approach but keys on the **full path after `/uploads/`** (keeps the month folder), so a same-name-different-folder swap now shows up as a real diff. Ran it site-wide and confirmed + fixed every instance the user's pages exposed:
- [x] `/doors/communal-entrance-doors` — "Custom doors" card background (`Mask-group-26.png`) pointed at `/2025/03/` (a black garage-style door) instead of `/2025/01/` (the correct white arched double door). Downloaded the correct file, fixed both the `data-bg-url` and inline `style` references. Confirmed match against live site both by path and by visual download comparison.
- [x] `/doors/panic-room-doors` — two background images both pointed at the wrong month folder: `Mask-group-24.png` (`/2025/03/`, a decorative security-grille window) should be `/2025/01/` (correct: empty hallway with a panic-room door ajar); `Mask-group-27.png` (`/2025/03/`, wrong content) should be `/2025/01/` (correct: 3-photo door collage). Both downloaded from WP and fixed. Screenshotted both spots post-fix — hero background and the door-collage section both now match the live site.
- [x] `/grilles-shutters/colllabsible-grilles` — `image-17.png` (used twice), `image-18.png`, `image-19.png`, `image-20.png` all pointed at `/2025/01/` instead of the live site's `/2025/02/` — visually confirmed `image-17.png` alone was a completely different photo (a building facade vs. the correct bay-window security grille). Downloaded all 4 correct files from WP and fixed all references. Screenshotted the hero — now shows the correct grille photo.
- [x] `/windows/security-aluminium-windows` — re-checked the "Bullet-resistant systems" section image (`Mask-group-16.png`) the user flagged: this was **already fixed** in the previous round of work this session (path and byte-for-byte content now match the live site exactly, confirmed via direct download comparison and a fresh screenshot). No further action needed — likely the user was looking at a stale/cached view before this fix had been visually re-verified.

`tsc --noEmit` clean after all edits. Re-ran `compare_all_images_strict.js` afterward: all 4 pages now PASS. Not committed/pushed yet.

### Same bug class found elsewhere, not yet fixed (not part of user's report this round)
Running the new strict script site-wide surfaced several more same-shape "wrong folder" and "extra/missing image" diffs the user has not reported yet. Not touched this round — flagging so they're not lost:
- [ ] `/doors/profile-doors` — `Mask-group-15/16/17/18.png` all resolve under `/2025/03/` on our page with no matching reference on the live site at all (11 body images on ours vs 7 on WP) — looks like extra/duplicate elements rather than a simple folder swap, needs its own investigation before touching.
- [ ] `/grilles-shutters/security-shutters` — missing 5 "Profile Dimension" spec images (`1-pa_52`, `2-PA-52-1`, `3-pa_55`, `4-entry-level`, `5-Maximum-dimensions`, all under `/2025/02/`) — already logged below under "Already known, not new" as living inside a WordPress popup/modal, deliberately not reconstructed.
- [ ] `/door-styles`, `/door-styles/edwardian-doors`, `/door-styles/french-doors`, `/door-styles/georgian-doors`, `/door-styles/victorian-doors` — each has one body-image count mismatch (4 vs 3, or a differently-named file entirely, not a folder swap) — pre-existing per the "False positives" section below for 3 of these; worth a fresh visual check before assuming false-positive still holds, since that check predates this session's better strict script.
- [ ] `/doors/fire-resistant-doors` — header image count mismatch (4 vs 1) — already investigated and dismissed as a script mis-categorization (gallery images, not header) in the "False positives" section below.
- [ ] `/blog` — one missing footer/related-post logo image — already flagged as low-confidence/possibly-flaky in the "Needs another look" section below.

## Regression: "PRODUCTS" menu stopped opening on desktop and mobile — 2026-09-11 — FIXED

Self-inflicted regression from earlier in this same session's mobile-menu work: the desktop "Products" button and the mobile off-canvas trigger both need to open the same shared panel (it doubles as the desktop mega-menu and the mobile nav - see the portal comment in `Header.tsx`), but `Header.tsx`'s click listener used `document.querySelector` (singular) to find "the" trigger button. Once the two buttons were given different `href` values (`#products-panel-trigger` for desktop, `#mobile-menu-trigger` for mobile) so the mobile one could be found at all, `querySelector` could only ever wire up one of them - breaking the other. Fixed by switching to `querySelectorAll` and attaching the click handler to every matching trigger element, and updating the "click outside closes the menu" check the same way. Verified both: desktop Products button opens the full mega-menu (screenshotted), mobile off-canvas icon opens the same panel in its single-column mobile layout (screenshotted), zero console errors. `tsc` clean. Not committed/pushed yet.

## Homepage "Luxurious Security Doors" video not showing — 2026-09-11 — FIXED

User reported the video beside the "Luxurious Security Doors" heading wasn't loading, even after leaving the page open for hours (ruling out a slow-load explanation). Visually confirmed via screenshot: the video's whole column was blank white with a permanently-spinning loading icon.

Found two independent, compounding bugs on this one video (`Untitled-2024-11-11-17-47-13copy-2-compressed.mp4`, `app/page.tsx`'s "tfs-slider" section):
1. **The slide itself was `display: none`.** This is Avada's FlexSlider-pattern widget - its real CSS hides every `<li>` inside `.slides` by default and only reveals the active one via a class that FlexSlider's own JS adds, which this migration never loads. Confirmed via `grep` that this is the *only* `className="slides"` element on the whole site (single slide, not a real carousel), so forcing it permanently visible is the correct fix, not a workaround. This was the primary cause - even before checking the video's own attributes, the entire slide (video included) was collapsed to 0x0 and invisible.
2. **The `<video>` tag itself was also missing the native `autoplay` attribute** - it only had the inert `data-autoplay="yes"` data attribute (meant for the same absent FlexSlider JS to read and manually call `.play()`), not real HTML `autoplay`. Even after fixing bug #1, the video would have sat there paused indefinitely with no user interaction. Added `autoPlay` and `playsInline` (the latter so mobile Safari doesn't force fullscreen instead of the inline background-video look every other hero video on the page already has).

Fix: `.tfs-slider .slides li { display: block !important; }` in `app/layout.tsx`'s global override block, plus `autoPlay={true}` / `playsInline={true}` on the `<video>` in `app/page.tsx`. Verified: video now renders a real frame (previously blank), has real non-zero dimensions, and both `autoplay`/`playsinline` attributes are present. Could not visually confirm active playback in this automated browser session specifically because of a already-documented environment limitation (`document.visibilityState === "hidden"` in a backgrounded/unfocused tab suspends video autoplay browser-wide) - confirmed this isn't specific to this video by checking that 4 *other*, already-correctly-configured videos on the same page show the identical paused/suspended state in this same session. `tsc` clean, zero console errors. Not committed/pushed yet.

- [ ] **Related, not yet fixed: `Secure-reviews-compressed.mp4`** (a different video elsewhere on the homepage) is also missing `autoplay`, and is also not muted - adding `autoplay` alone wouldn't be enough (browsers block autoplay on unmuted video) and unlike the two videos above, this one may be *intentionally* click-to-play (e.g. a customer testimonial with real audio) rather than a silent background loop - needs the user's call on intent before touching it, not assumed to be the same bug.

## Sitewide image audit vs secure-house.co.uk — 2026-09-11

User asked for a scripted check of every page's images (header + body) against the live WordPress site, after noticing mismatches on a few pages manually. Wrote `scripts/compare_all_images.js` (fetches both sites' raw HTML per page, extracts real image URLs accounting for the live site's lazy-loading - `data-orig-src`/`data-src` fall back correctly even without a browser - plus `data-bg-url` background images, and diffs the filename sets). Ran across all 36 sitemap routes: 23 matched cleanly, 13 flagged a difference. Verified every flagged page by hand (file-size comparison against the real URLs, not just filename-guessing) rather than trusting the script's raw output - several flags turned out to be false positives.

### Confirmed real image mismatches — needs fixing
- [x] **`/doors/communal-entrance-doors` — wrong images, confirmed visually, not just by file size — FIXED (2026-09-11).** Downloaded and looked at both pairs directly: our `Mask-group-24.png` shows a decorative security-grille window; the live site's `Mask-group-24-1.png` shows a split photo of two modern communal entrance doors (blue glass + wood). Our `Mask-group-27.png` shows a garage door; the live site's `Mask-group-27-1.png` shows a wooden double door with glass panels. These are genuinely different photos, not a rename or a resized variant - real wrong-image content on a live page. All 4 mismatches (`Mask-group-24-1`, `-27-1`, `-29`, `-30`) fixed in the first pass this session; a 5th, `Mask-group-26.png` pointing at the wrong month folder (`/2025/03/` instead of `/2025/01/`), found and fixed in the follow-up pass (see "Follow-up image audit" section at the top of this file). Page now matches the live site path-for-path.
- [x] **`/garage-doors/sectional-garage-doors`** — moved to "False positives" below, see that section.
- [x] **`/garage-doors/sliding-garage-doors`** — moved to "False positives" below, see that section.
- [x] **`/windows/security-aluminium-windows` — FIXED (2026-09-11).** Our `Mask-group.png` (641KB) vs the live site's `Mask-group-8.png` (583KB) - confirmed different file, downloaded the correct one from WP and fixed the reference. Also fixed the separate already-known `Mask-group-16.png` blurry/wrong-resolution bug on this same page (was a tiny 3,652-byte stretched thumbnail from `/2025/04/`, replaced with the correct 973,422-byte full-res file from `/2025/02/`, matching the live site exactly). Both re-verified visually in the follow-up pass — byte-for-byte identical to the live site's files.
- [ ] **`/doors/profile-doors`** — loads the full-size `Mask-group-15/16/17/18.png` in addition to the `-227x300` thumbnail crop the live site actually displays - likely a bandwidth/performance issue (serving a much larger file than needed) rather than a wrong-image issue; worth checking whether the on-page rendered size matches what's intended.

### False positives — verified, no action needed
- [x] **4 door-styles pages (edwardian, french, georgian, victorian) flagged their parallax hero image as "extra on ours."** Not a bug - confirmed the live site injects this image purely via JS at runtime (Avada's parallax script), so it never appears as a static `data-bg-url` attribute in the live site's raw HTML for the script to find, even though it displays correctly once JS runs. This is the same image already fixed and visually verified earlier in this log ("Missing parallax background images" section).
- [x] **`/door-styles` Downing Street image** flagged as a filename mismatch - confirmed identical file (860,580 bytes on both sides), just renamed during the original scrape (the live filename is a very long, awkward one). Same content, no fix needed.
- [x] **`/doors/fire-resistant-doors` "header" mismatch** - a false categorization by the comparison script, not a real header problem: the flagged images (`NCC_03`, two `Billede-...` files) are actually gallery photos (`class="fusion-gallery-image"`), misclassified as header images by the script's crude ancestor-based heuristic.
- [x] **`/garage-doors/sectional-garage-doors`** (`sectional-garage-door-and-facade-2.jpg`, 217KB, vs the live site's `sectional-garage-door-and-facade.jpg`, 2.6MB) **and `/garage-doors/sliding-garage-doors`** (`Ryterna-Trends-side-sliding-garage-door-1.jpg`, 222KB, vs `...-door.jpg`, 2.4MB) — both downloaded and viewed directly: same photograph in both cases, just a different crop (a wide banner crop vs. the full uncropped image) — the large file-size gap is fully explained by the crop + compression difference, not different content. Deliberately left untouched.

### Already known, not new
- [x] `/grilles-shutters/security-shutters` missing 5 "Profile Dimension" spec images - already documented above (external audit review section): these only exist inside a WordPress popup/modal on the live site, deliberately not reconstructed (bigger scope than a content fix).

### Needs another look, low confidence
- [ ] `/blog` flagged one missing image (`avada_logo-3.png`) - could not reproduce on a second fetch; the blog listing likely shows rotating/dynamic "related posts" thumbnails that differ between requests, making this comparison inherently flaky for this one page. Not treating as confirmed until re-checked deliberately (e.g. multiple fetches to see if it's genuinely random).

## Homepage mobile responsiveness — 2026-09-11 — good enough for now, polish later

User asked to make the homepage mobile responsive. Fixed the launch-blocking issues: hero heading was rendering at 50px instead of 34px (a legacy Avada `.post-content h1` rule with higher specificity than the CSS module's own responsive rule was winning); the mobile hamburger menu didn't exist at all (the real off-canvas trigger anchor was scraped but never wired up or given an icon - fixed by pointing it at Header.tsx's existing open/close logic); the full-screen menu panel's 4-column layout and separately-positioned contact bar were both hardcoded desktop-only inline styles with no mobile fallback, causing severe overlap - now stack into one column. Verified: full hero, 4-cards, gallery, footer, and the complete open/scroll/close menu flow all screenshotted end-to-end at 390px width, zero console errors, zero horizontal scroll, desktop confirmed unaffected. **User says it "feels ok for now"** - treating as functional but not final polish. Not committed/pushed yet.

- [ ] **Revisit homepage mobile responsiveness for final polish** (not launch-blocking, current state is acceptable). Only the homepage has been audited/fixed this pass - every other page still has the same untouched issues (hero/heading sizing on other templates, any other JS-driven Avada widgets at mobile width) since this was explicitly scoped to the homepage only. When resumed: audit remaining pages the same way (real ~390px viewport via the iframe-injection workaround - `resize_window` does not actually change viewport width in this environment), and do a closer visual pass on the homepage itself (spacing/sizing polish beyond "not broken").

## FAQ / Read more / See more audit — 2026-09-11

User asked to verify everything in `FAQ_AND_MORE_BUTTONS_DIRECTORY.md` (11 FAQ sections/99+ questions, 42 "Read more" toggles, 16 "See more" gallery buttons, 3 "Learn more" links) actually works.

- [x] **Critical sitewide bug found and FIXED — `components/AccordionToggle.tsx` silently broke any FAQ/Read-more/Learn-more toggle whose target id starts with a digit.** The click handler called `document.querySelector(targetSelector)` where `targetSelector` is a content-hash id like `#8b62cb6caf25eab40` — `querySelector` throws a `SyntaxError` on an id selector starting with 0-9 (invalid CSS), and since that throw happens before the `e.preventDefault()` line, the whole handler silently failed: no panel opened, and the browser fell through to its default `href="#id"` behavior (a dead anchor-jump) instead. Of the 42 "Read more" toggles alone, 19 target ids start with a digit — all were broken; likely a similar share of the 99+ FAQ answers too (confirmed 9/11 broken on `/doors/bullet-proof-doors` before the fix). Root-caused via live DOM testing (dispatched real click events, checked `defaultPrevented` and before/after `.in` class state), not assumption. **Fix**: swapped to `document.getElementById(targetSelector.slice(1))`, which has no such restriction. Verified after the fix: all 3 toggle types work on `/doors/communal-entrance-doors` (FAQ, Read more, Learn more), including the accordion's sibling-close behavior; all 11/11 FAQ questions on `/doors/bullet-proof-doors` now toggle correctly. `tsc` clean. **Not committed/pushed yet** — awaiting go-ahead.
- [ ] **"See more" gallery buttons (16 total) are non-functional — but this is inherited from the live site, not a migration regression.** Confirmed: plain `<a href="#">See more</a>` with no `data-toggle`, no `onClick`, no handler anywhere in the codebase (`awb-gallery-load-more-btn` isn't referenced by any component). Clicking does nothing but jump to the top of the page. Checked whether this hides real additional photos: compared image counts before/after clicking on both our production-equivalent page and the **live WordPress site itself** (`/door-styles/edwardian-doors`, 7 images) — identical count before and after on both sites. The live site's own "See more" button doesn't load anything either. Not a content-loss bug; left as non-functional to match the original unless the user wants it built into a real feature (would be new scope, not parity).
- [x] **Plain "Learn more about us" link (homepage hero, `HeroSlider.tsx`)** — real `href="/about-us/"` navigation link, not a toggle. Confirmed correct, no issue.

## Missing parallax background images — 4 door-styles pages — 2026-09-11 — FIXED

User reported no image showing right after the intro paragraph on Georgian, Victorian, Edwardian, and French Doors. Confirmed real: each of these 4 pages (and only these 4 — verified via `grep -rl "fusion-bg-parallax" app` returning exactly this set) has a `.fusion-fullwidth.fusion-parallax-up` section right after the intro text with a `data-bg-url` attribute but no actual `background-image` CSS — Avada's real parallax JS would normally read that attribute and apply the image at runtime, and this migration never pulls that JS in. Root-caused via the page's own compiled CSS (`fusion-styles/*.min.css`): `.fusion-fullwidth` already resolves `background-image: var(--awb-background-image)` (default `none`) — the same CSS variable pattern already used successfully elsewhere in this codebase (e.g. the homepage hero, `--awb-background-size`/`--awb-background-position` already default to `cover`/`center center` so only the image itself was missing). **Fix**: added `'--awb-background-image': 'url("...")'` to each section's existing inline `style` object, reusing the same `data-bg-url` path already present in the markup — no new assets needed. Verified visually and via `getComputedStyle` on all 4 pages: Georgian shows a purple front door with white brick facade, Victorian/Edwardian/French each resolve their correct background image. `tsc` clean. **Not committed/pushed yet.**

## Security Shutters converted to real JSX (the last raw-HTML page) — 2026-09-11

`/grilles-shutters/security-shutters` was the one page out of all 35 approved pages never converted from `dangerouslySetInnerHTML` to real JSX during the original migration effort — no documented technical reason found in this log for why it specifically was skipped; most likely it was simply the largest remaining `content.html` (384 KB) and conversion work never got back to it.

Converted using the exact same pipeline already proven on the other 34 pages (`scripts/html_to_jsx.js` → `scripts/assemble_jsx_page.js`, pilot at `-v2`, verify, swap):
- Structural diff against the original wrapper: 0.128% text delta (well under the project's 1% pass threshold), identical `<img>` count (18), background-image count (13), and `<a href>` count (57).
- `tsc` clean, zero console errors.
- Verified all interactive elements still work post-conversion: FAQ accordion, "Read more" toggle, and the gallery "See more" button (this page has all three, plus 10 Read-more toggles per `FAQ_AND_MORE_BUTTONS_DIRECTORY.md`) — all confirmed via direct DOM state checks (before/after class toggling), not just visual inspection.
- Old raw-HTML version preserved (not deleted) at `/legacy/grilles-shutters/security-shutters`, matching the pattern used for all previously-converted pages — confirmed it still renders correctly, and is still excluded from the sitemap / disallowed in robots.txt via the existing generic `/legacy` rule (no changes needed there).
- Sitemap count unchanged at 37 URLs (this page was already live/indexed — only its internal implementation changed).

Now 35/35 approved pages are real JSX; zero pages left using `dangerouslySetInnerHTML` for their own content (the `/legacy/*` tree is intentionally excluded/preserved-as-reference, not counted).

## CEO pre-launch review — 2026-09-11

CEO-requested review (via user) against 3 points + a 5-item AI checklist (checklist wrongly assumed Django — this is Next.js/Vercel, no backend). Full artifact: https://claude.ai/code/artifact/c6803ff6-6a01-46f3-8c03-c141c6e3a048. Not fixed yet — review/record-notes only.

### To do — confirmed real, needs fixing
- [ ] **Contact form has no backend — BLOCKER.** `app/contact-us/page.tsx` (`action="/contact-us"`) has no `onSubmit`, and there is no API route or server action anywhere in the codebase. It is the only `<form>` on the whole site (verified via `grep -c "<form" app/*/page.tsx app/*/*/page.tsx`). Submitting it currently does nothing and sends no email. Needs a real submission endpoint (with rate-limiting/validation once built) before launch.
- [ ] **Header confirmed genuinely non-sticky** (resolves the earlier "not investigated yet" note below). Live-tested: `.fusion-sticky-container` stays `position: absolute` at every scroll depth (`rectTop: -1500` after scrolling 1500px); `Header.tsx` has zero scroll listeners. Avada's sticky-header markup attributes exist but were never wired up in the migration. Needs a real implementation across all pages.
- [ ] **No custom 404/500 pages** — visitors see Next.js's generic defaults.
- [ ] **No redirects configured** (`next.config.ts` has no `redirects()`) — real SEO exposure for the ~150 pages moved to `_archive/` in the September scope-reduction; their old live URLs now 404 with nothing forwarding them. Needs a decision: redirect them, or explicit sign-off to accept the loss.
- [ ] **No security response headers** (CSP, X-Frame-Options, X-Content-Type-Options, Referrer-Policy) — standard, cheap hardening not yet done. (HTTPS/HSTS itself is confirmed fine — 308 redirect + `Strict-Transport-Security` header present on production.)
- [ ] **Mobile responsiveness untested this pass** — `resize_window` didn't change `window.innerWidth` in this automation session (stuck at 2560); viewport meta tag is correct but visual behavior needs a real-device check, not assumed either way.
- [x] **"Scroll section" cards rendering squished/broken sitewide — FIXED.** Affects Communal Entrance Doors, Security Aluminium Windows, Colllabsible Grilles, Tracless Garage Doors, and the Grilles/Shutters hub (all pages using Avada's `fusion-scroll-section awb-swiper-full-sections` widget). Two rounds:
  1. First attempt (commit `598b741`) only fixed the grid-squish (the site's generic `.swiper-wrapper { display: grid; grid-template-columns: repeat(auto-fit, minmax(250px,1fr)) }` fallback was squeezing full-width slides into ~250px columns). Verified on the dev server, but the user canceled the Vercel deploy before it finished and asked to revert rather than retry — reverted in `f8ae692`, so it never reached production.
  2. On reapplying, the user reported the fix had actually been overlapping/underlapping sections on top of each other. Investigated further and found the real cause in Avada's own compiled per-page CSS (`fusion-styles/*.min.css`): each card's outer `.hundred-percent-height` wrapper is clamped to `height: calc(100vh - adminbar-height)` regardless of content, so taller cards overflowed their box and the next card started inside that overflow. The first fix never touched this. Corrected fix replicates Avada's own built-in no-JS fallback rules for this exact widget (`.awb-swiper-full-sections { height: auto }`, `> .swiper-wrapper { display: block }`, `.hundred-percent-height { height: auto }`) instead of guessing. Verified this time with a script measuring every card's actual bounding box and checking for overlap on all 5 pages (zero overlaps) plus the section immediately following the cards on each page (clean gap, no overlap) — not just visual spot-checks.
- [ ] **Security Aluminium Windows: `Mask-group-16.png` background image renders heavily blurred/upscaled** just above the "Bullet-resistant systems for the protection of people and property" heading — a CSS `background-image` on a `.fusion-column-has-bg-image` column, not an `<img>` tag. The live WordPress site shows a crisp sofa/patio photo in this spot; production shows a pixelated, wrong-resolution version. Likely a low-res source file was picked up during the asset migration instead of the full-size original. Not yet fixed.

### To do — confirmed real, needs fixing (from earlier audit)

User supplied a third-party audit doc (`secure-house-35-page-deep-audit.docx`, dated 10 Sep 2026, checked WordPress vs the deployed production site) and asked whether it was correct. Independently verified every claim against the actual production site (`secure-house-next-js.vercel.app`), our own source (`content.html`/`page.tsx`), and the live WordPress site directly, rather than trusting it at face value. Findings below — **not fixed yet, just documented per user's request ("mark it, note it down").**

### To do — confirmed real, needs fixing
- [ ] **Door Styles hub (`/door-styles`) has zero working links to its 4 children** (French/Victorian/Edwardian/Georgian Doors). Confirmed: live WordPress has real `<a href="https://secure-house.co.uk/door-styles/french-doors/">` etc. on these cards; our scraped `content.html` never captured them at all (zero occurrences of any child-page path in either `content.html` or `page.tsx` — not a JSX-conversion bug, a scrape-time gap). Small, contained fix: add the 4 links back.
- [ ] **4 profile-door product pages are missing real content**, confirmed for all 4 (Fuego Fire, Presto Bullet Proof, Stainless Steel, Unico Slim Line): the live WordPress site has downloadable PDF spec sheets organized by fire rating (EI30/EI60/EI90/EI120) and a proper "Get a Quote" contact form on each; our scraped `content.html` has **zero PDFs and zero `<form>` elements** on any of the 4 (verified via direct grep on all 4 files). Needs sourcing the PDFs and form markup from the live site and adding them — bigger than the Door Styles fix, not started.

### Investigated — see "CEO pre-launch review" section above
- [x] **Header sticky behavior** — was a bare note, now fully investigated and confirmed broken (genuinely not sticky at all). See the "CEO pre-launch review" section at the top of this file for the verified finding.

### Already known/tracked, not new
- [x] **Security Shutters (`/grilles-shutters/security-shutters`) still uses `dangerouslySetInnerHTML`** — true, but this is the one page never converted to JSX yet, already on the project's own list. Not a surprise finding.

### Pre-existing content bugs on the live WordPress site itself — confirmed real, left as-is per project precedent
- [x] **Georgian Doors (`/door-styles/georgian-doors`) gallery heading literally says "Edwardian door gallery"** — confirmed via direct fetch of both our production page and the live WordPress page: **both** show the identical mislabeled heading. Client's own existing content error, faithfully preserved, not a migration defect. Matches how this project has already handled other live-site typos (e.g. "Frequently asked uestions") — left alone unless the client asks to fix it.
- [x] **Colllabsible Grilles (`/grilles-shutters/colllabsible-grilles`) has a duplicated sentence** ("To put it simply, security grilles are an additional security measure...") appearing twice back-to-back with no break. Confirmed present identically on the live WordPress site. Same situation — pre-existing, not introduced by the migration.

### Debunked — do not act on these, flagging so nobody else does either
- [ ] ~~**"Critical: unrelated casino/gambling spam content on the production homepage, should block release."**~~ **FALSE.** Fetched the raw production homepage HTML directly and searched for casino/gambling/betting keywords — zero matches. A second independent check confirmed the page is clean. Almost certainly the audit tool confusing our site with the **live WordPress site's** own separate, already-documented spam-injection issue (see `MISTAKES-AND-PATCHES.md` items 7/13/17) — that issue has never existed on any Next.js page in this project.
- [ ] ~~**"Homepage navigation items rendered as plain text, not real links."**~~ Checked the production homepage's main nav directly — found 8 real working `<a href>` links for the primary nav items (Products/Doors/Windows/Garage Doors). No evidence found supporting this claim.

## `/new/*` vs live WordPress site — earlier full-site audit (pre-swap)

Full audit of all 34 converted pages against secure-house.co.uk, done 2026-09-10. Method: `get_page_text` comparison of each `/new/*` page against its live WordPress counterpart, with targeted screenshots where text alone was ambiguous. Header/footer/nav were not checked per-page (shared components, already verified separately). Full URL list: `migration-log/PAGE-COMPARISON.md`.

**28 of 34 pages: no changes needed, exact match against live site.**

## Resolved (user decisions) — 2026-09-10

### Projects (`/projects`) — keep as-is, no change
User's call: keep our correct category labels (PROJECTS / DOORS / GRILLES, SHUTTERS) rather than matching the live site's broken "Your Content Goes Here" placeholder.

### Communal Entrance Doors (`/doors/communal-entrance-doors`) and Colllabsible Grilles (`/grilles-shutters/colllabsible-grilles`) — false alarms, root cause found and fixed
Both flagged by the audit as having "extra" content not on the live site (a 4-card "Versatility for every property type" section on Communal Entrance Doors; a CX1/CX2/VULCAN/ECLIPSE grille-tier spec section on Colllabsible Grilles). **Turned out to be a false alarm, not missing/extra content**: both sections use Avada's "scroll-stack" Swiper widget (`data-animation="stack"`, wrapped in `.fusion-scroll-section`), which normally reveals its cards progressively as the user scrolls, driven by real Swiper JS that this static migration never pulls in (see `DECISIONS.md`, "the Swiper carousel decision" — `/doors/communal-entrance-doors` is explicitly named there as an example). The audit's `get_page_text` check on the live site simply couldn't see cards that only reveal via scroll-linked JS animation — the content is real and present on both sides.

User reported the actual visible symptom directly: cards render side-by-side in a static grid with zero animation (matches the existing global CSS override in `app/layout.tsx` that forces `.swiper-wrapper` into a plain grid, chosen as a "flat but readable" fallback since full Swiper was never installed). Asked the user whether to leave this as-is, add a lighter CSS-only scroll-in reveal (the middle-ground option `DECISIONS.md` already anticipated), or install real Swiper for the exact stack/rotate motion — **user chose the CSS-only middle ground for both pages**.

**Fix**: added `components/ScrollReveal.tsx` (a small client component using `IntersectionObserver`, re-running on every route change via `usePathname()` since the root layout doesn't remount on client-side navigation) plus matching CSS transition rules in `app/layout.tsx` (`.fusion-scroll-section .swiper-slide` starts faded/translated down, gets `.scroll-revealed` added as it scrolls into view). Mounted globally in `app/layout.tsx`, so it applies automatically to **every** page using this pattern, not just these two: `/doors/communal-entrance-doors`, `/grilles-shutters/colllabsible-grilles`, `/garage-doors/sectional-garage-doors`, `/garage-doors/tracless-garage-doors`, `/grilles-shutters/` (hub), `/windows/security-aluminium-windows`. Verified on both `/doors/communal-entrance-doors` and `/grilles-shutters/colllabsible-grilles`: cards fade/slide in on scroll, end fully visible, all 4 slides get `.scroll-revealed`. `tsc`/`build` clean. This is **not** a replica of the live site's exact "stack and rotate upward" motion (that needs real Swiper — bigger scope, not chosen) — it's real entrance motion instead of content just appearing, same spirit as the live site without the exact same mechanism.

**Side effect worth knowing**: this also resolves 2 of the "likely false alarm" items further down this doc (Tracless Garage Doors, Security Aluminium Windows) and the Grilles Shutters hub, since all three use the same `.fusion-scroll-section` pattern and were flagged for the same underlying reason (content that only reveals via scroll-linked animation, invisible to a text-scan check). Attempted to re-verify all 3 directly in-browser: confirmed the elements and markup are structurally identical to the 2 already-working pages, but hit a browser-automation artifact partway through (the automated tab got backgrounded, which Chrome throttles `IntersectionObserver` for — confirmed by testing that even `document.body`, which should always intersect, reported `isIntersecting: false` while `document.visibilityState === "hidden"`) rather than a real per-page bug. Not a concern for actual users (a real, focused tab doesn't have this throttling) — the underlying mechanism is proven on 2 pages and identical on all 6.

## Verified — FAQ accordions and "Read more" toggles (RESOLVED 2026-09-10)

User reported FAQ questions and "Read more" links not working, across multiple pages. Confirmed real and root-caused: full writeup in `MISTAKES-AND-PATCHES.md` item 25. Fixed globally via `components/AccordionToggle.tsx` — affects 20 pages. Verified directly: expand/collapse, chevron icon direction, and Bootstrap-style accordion-group behavior (opening one panel closes a previously-open sibling) all work correctly on `/doors/bullet-proof-doors`; the "Read more" variant of the same component verified working on `/garage-doors/sectional-garage-doors`.

## Known limitation, not fixable — Trade page gallery "load more" button

`/trade/`'s gallery has a `.awb-gallery-load-more-btn` ("See more") button that normally fetches additional gallery images via AJAX from a WordPress endpoint (`data-limit="3" data-page="1"` in the markup). This migration has no backend to fetch from, and only the first page's worth of images was ever scraped — there is no "page 2" data anywhere to restore. Unlike the accordion issue, this isn't a missing click handler for content that's already present; the content genuinely doesn't exist in this project. Left as-is (button present but non-functional, matching current behavior) rather than faking pagination or removing the button and changing the page's appearance without a content decision from the client.

## Fixed this session

### Sectional Garage Doors (`/garage-doors/sectional-garage-doors`) — RESOLVED 2026-09-10
The 4 product cards (R40 Sectional / TL Sectional / SHD Side Hinged Doors / SSD Side Sliding Doors) were rendering **twice**. Root cause: `content.html` itself has two full copies — a mobile-only-tagged copy (`fusion-no-medium-visibility fusion-no-large-visibility`) using a generic, undated placeholder image (`paveikslas.png`) for all 4 cards, and a second, unrestricted copy using real per-product photos (`paveikslas-89/90/92/94.png`, all uploaded the same date as the "restore missing section" fix logged in `PROGRESS.md`'s "Completed (continued, 4)" entry). Also confirmed the Avada `fusion-no-X-visibility` classes don't actually hide anything in this static export at any viewport width (same "JS-driven visibility that never runs here" pattern documented elsewhere in this project) — so this wasn't ever a working responsive show/hide, both copies rendered unconditionally on every page load. **Fix**: deleted the generic-placeholder-image copy from both `app/new/garage-doors/sectional-garage-doors/page.tsx` (399 lines) and the shared `app/garage-doors/sectional-garage-doors/content.html` (the same block, found via balanced-div matching since that file isn't pretty-printed), keeping the real-photo copy in both. Verified on both the old (`/garage-doors/sectional-garage-doors`) and new (`/new/garage-doors/sectional-garage-doors`) routes: heading appears exactly once on each, `tsc`/`build` clean, screenshot confirms a real garage door photo renders correctly.

## Missing metadata (already-known gap, blocked on client)

### Edwardian Doors (`/door-styles/edwardian-doors`) and French Doors (`/door-styles/french-doors`)
Browser tab title is generic "Secure House" instead of a real page title, because these pages have no `metadata` export at all (same gap Georgian/Victorian Doors had before their metadata was added this session — see `PROGRESS.md`'s "Blocked on Client" list). Needs real title + meta description copy from the client. Visible body content otherwise matches the live site exactly.

## Worth a quick follow-up (unverified / possibly false alarms)

### Homepage (`/`) — low confidence
The hero slider's active slide text at the moment of checking didn't match what WordPress's text extraction showed. Very likely just slider-timing (`get_page_text` only captures whichever slide is active at that instant on a multi-slide rotating hero) rather than a real missing slide — worth a quick manual click-through of all hero slides on both sites to confirm, not treated as a confirmed issue.

### Tracless Garage Doors (`/garage-doors/tracless-garage-doors`) — likely false alarm
Extra, duplicated-looking text near the "Surfaces" section ("Woodstyle colors / Woodgrain, slightgrain, smooth colors" appearing twice) that doesn't appear in the live page's extracted text. Not independently confirmed with a screenshot — likely the same class of thing as the Grilles Shutters hub false alarm below (a JS-driven color-swatch/tab component where the live site only shows the active swatch's caption and our static conversion renders all captions at once). Worth a quick visual check before treating as real.

### Security Aluminium Windows (`/windows/security-aluminium-windows`) — likely false alarm, unverified
4 extra "Security class WK1/WK2/WK3/WK4" definition blocks appear under "Security classes: secure yet unobtrusive" that don't appear in the live page's extracted text. A screenshot of the live page showed a blank area right where this content would be — consistent with a collapsed accordion/JS panel that never renders without interaction, not genuinely deleted content (different from the Colllabsible Grilles case above, which was confirmed fully absent even behind its toggle). Not fully verified — worth checking whether this section has a clickable toggle on the live page.

### Windows hub (`/windows`) — low confidence, not a conversion issue either way
The shared `CTABlock` component renders "Letâ€™s secure your property Together" (mojibake apostrophe) instead of "Let's" — confirmed identical on **both** the old and new Next.js routes, so this is a pre-existing encoding bug unrelated to the JSX-conversion effort, not a regression. It did not reproduce on the next page checked in the same session, so treat as possibly intermittent. Not urgent, but worth a wider spot-check across pages using `CTABlock` if it keeps showing up.

## Confirmed false alarms (already resolved during the audit, no action needed)

- **Grilles Shutters hub** (`/grilles-shutters`): initial text diff showed extra spec bullets on `/new/`, but a screenshot of the live page confirmed it's a real 2-slide slider present on both sites — `get_page_text` just didn't capture the inactive slide.
- Pre-existing typos already on the live WordPress site itself (not migration errors, correctly preserved as-is): "Frequently asked uestions" (Bullet Proof Doors), "Maximum Dimenions" (Fire Resistant Doors), "Unico slime line" (Profile Doors hub).
- Live WordPress's "Your Content Goes Here" placeholder on the homepage's "Our Projects" cards — a known, already-investigated live-site WordPress quirk; our page correctly omits it.

## Pages with no changes needed (exact match)

About Us, Doors hub, Bullet Proof Doors, Fire Resistant Doors, High Security Doors, Panic Room Doors, Profile Doors hub, Fuego Fire, Presto Bullet Proof, Stainless Steel, Unico Slim Line, Door Styles hub, Georgian Doors, Victorian Doors, Garage Doors hub, Side Hinged Garage Doors, Sliding Garage Doors, High Security Shutters, Products, Security Levels, Trade, High Security Steel Windows, Contact Us.

(Raw per-page audit notes, including full method details, are in `migration-log/audit-batch-1.md`, `audit-batch-2.md`, `audit-batch-3.md`.)
